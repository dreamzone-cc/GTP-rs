# GTP-rs — الورقة التقنية المتكاملة: تدقيق التكامل بين الطبقات، ثوابت النظام، ومواصفة محرك التوجيه التكيفي

**رقم الوثيقة:** GTP-rs-ICD-01
**الإصدار:** 1.0
**التاريخ:** 31 أغسطس 2026
**الأساس المُدقَّق:** `integration/all-fixes` عند **`1c0e488`** — تحقّق مباشر من الشجرة المحلية
**يحلّ محل / يوحّد:** `GTP-rs_Comprehensive_System_Audit_and_Testing_Report_AR.md` (التدقيق) + `GTP-rs_Deep_Study_Routing_Engine_AR.md` (دراسة المحرك) + `Technical_Paper_Gaming_VPN_Adaptive_Routing.md` / `GTP++.md` (الورقة المفاهيمية)
**القيد الحاكم:** رفع الأداء **دون زيادة ملموسة في الكلفة الحوسبية أو التعقيد التشغيلي داخل النواة**، مع بقاء محرك التوجيه **مكوّناً مستقلاً بالكامل**.

---

## 0. بطاقة الوثيقة

### 0.1 لماذا هذه الورقة

الوثائق الثلاث السابقة صحيحة كلٌّ في نطاقه، لكنها **لا تتحدث مع بعضها**. النتيجة ثلاث فجوات هيكلية:

1. **فجوة التصنيف:** التدقيق يقيس الخطورة في سياق بروتوكول ساكن. الدراسة تقترح محركاً يهاجر المسار حتى مرتين في الدقيقة. **ثلاثة بنود مصنّفة «منخفضة» أو «اختيارية» تنقلب إلى سدّية لحظة تشغيل المحرك** — ولا أحد رصد الانقلاب.
2. **فجوة التكامل:** كلا التقريرين يدقّق **الطبقات**، ولا أحد يدقّق **الدرزات بينها** بوصفها كياناً مستقلاً له ثوابته الخاصة. وكل العيوب الحرجة الأربعة المرصودة (N-1…N-4) هي — بلا استثناء — **انتهاكات لثوابت عابرة للطبقات**، لا أخطاء داخل طبقة.
3. **فجوة القياس:** نموذج القياس في دراسة المحرك (استقصاء 1Hz) **لا يستطيع بنيوياً** إنتاج المقياس الذي يزنه بـ0.25 في دالة القرار.

### 0.2 ما تضيفه هذه الورقة

| # | الإضافة | القسم |
|---|---|---|
| 1 | **إطار ثوابت التكامل (INV-1…INV-20)** — لغة موحّدة لوصف الدرزات، وربط كل عيب معروف بثابت منتهَك | §4 |
| 2 | **جرد الحالة المشتركة ومصفوفة الاقتران** — من يملك كل قيمة، ومن يقرؤها عبر حدود الطبقات | §3 |
| 3 | **تحقّق مباشر من 9 بنود حرجة** ضد `1c0e488` — إزالة كل الافتراضات | §2.3 |
| 4 | **19 نتيجة تدقيق جديدة (X-1…X-19)** لم ترصدها أي جولة سابقة | §5 |
| 5 | **إعادة تصنيف الخطورة تحت افتراض تشغيل المحرك** | §5.3 |
| 6 | **مواصفة تنفيذية كاملة لتسع آليات (RE-1…RE-9)** بخوارزمياتها وحدودها العددية | §7 |
| 7 | **إعادة تعريف ميزانية زمن القرار** إلى ثلاث فئات بعتبات منفصلة — يحلّ التناقض المركزي في الدراسة | §7.6 |
| 8 | **آلية «التحقّق بعد التبديل»** — تعالج فجوة ECMP التي تُبطل صحة أي قياس خارج النطاق | §7.7 |
| 9 | **خطة تنفيذ ببوابات G0…G6** ومصفوفة تتبّع كاملة | §8، §10 |

### 0.3 الخلاصة التنفيذية

> **النظام سليم في جوهره الخوارزمي، ومعطوب في درزاته.** الخوارزميات مطابقة لمعاييرها (RFC 1982 / 9002 / 8312)، لكن **كل العيوب الحرجة تقع على حدود الطبقات** — حيث لا يملك أي اختبار وحدة سلطة الرصد.

> **الأصل الأثمن للمشروع موجود ومهدور:** الترويسة تحمل `timestamp_micros: u32` في كل حزمة، **داخل الـAAD (موثَّق تعميرياً)**، يُكتب في TX ولا يُقرأ في RX إطلاقاً — **تحقّق مؤكَّد: صفر مستهلك خارج الأداة التشخيصية**. قراءته وحدها تسدّ الفجوة القياسية التي تفصل محرك التوجيه المقترح عن قابليته للعمل.

> **الترتيب الصحيح للتنفيذ ليس «إصلاح ثم بناء»، بل «إصلاح الدرزات التي يعتمد عليها القياس، ثم القياس، ثم القرار، ثم التنفيذ».** أي محرك يُبنى قبل إصلاح `min_rtt` وN-1 سيتخذ قرارات صحيحة رياضياً على مدخلات فاسدة.

---

## 1. النطاق والمنهج ونموذج الثقة

### 1.1 النطاق

| داخل النطاق | خارج النطاق |
|---|---|
| 13 حزمة في `crates/` عند `1c0e488` | مصادقة الخادم (قرار معماري SEC-A7 مؤجّل) |
| الدرزات بين الطبقات وثوابتها | تصميم بنية VPS/الشبكة الفيزيائية |
| آليات الورقة المفاهيمية M1–M14 | التنبؤ بالتعلم الآلي / مراقبة BGP (مرفوض داخلياً) |
| مواصفة محرك التوجيه وتنفيذه | تعدّد المسارات المتزامن داخل الاتصال (مؤجّل بقرار) |
| خطة الاختبار والتحقق | إعادة توليد تقارير `docs/` (بند حوكمة منفصل) |

### 1.2 المنهج

1. **تحليل الدرزات (Seam Analysis):** بدل تدقيق كل طبقة على حدة، جُرِدت كل قيمة حالة تعبر حدود طبقة، وحُدّد مالكها وقُرّاؤها وكُتّابها، ثم اشتُقّ من كل عبور **ثابت تكامل** قابل للاختبار.
2. **تحقّق مباشر من الكود:** كل ادعاء في §2.3 مُثبت بـ`grep`/قراءة مباشرة من الشجرة المحلية عند `1c0e488` — لا استنتاج ولا اعتماد على وثيقة.
3. **تدقيق عكسي متبادل:** كل وثيقة سابقة دُقّقت في ضوء الأخرى — وهو مصدر أثمن النتائج (§5.3).
4. **تحليل الأثر المتقاطع:** لكل آلية مقترحة، تُحدَّد الطبقات التي تلمسها والثوابت التي تُجهدها والثوابت الجديدة التي تفرضها.
5. **قاعدة الحوكمة الموروثة:** *«بند بلا اختبار = غير منفَّذ»*.

### 1.3 نموذج الثقة في الوثائق المرجعية

| المصدر | درجة الثقة | التبرير |
|---|---|---|
| الكود عند `1c0e488` | **مرجع نهائي** | كل تعارض يُحسم لصالحه |
| `GTP-rs-Technical-Specification.md` | **موثوق** | مصفوفة §12 مطابقة للواقع (تحقّق التدقيق) |
| تقارير `fixes/` | **موثوقة مع تصحيحات نقطية** | §5.2 يعدّد التصحيحات |
| `docs/specs/GTP-*-01.md` | **غير مدقّقة — نطاق مفتوح** | 10 مواصفات معيارية خارج نطاق الـ55 مطالبة (X-18) |
| تقارير `docs/` الثلاثة «الشاملة» | **غير صالحة كدليل** | أحكام مطبوعة ثابتة، اختبارات مفبركة |
| `GTP++.md` / الورقة المفاهيمية | **مصدر أفكار لا مصدر وقائع** | ستة أخطاء وصفية مصحَّحة |

---

## 2. البنية الطبقية — الخريطة المرجعية

### 2.1 الطبقات ومسؤولياتها

```
L5  التطبيق / الواجهة        gtp (facade) · gtp-cli
    ─────────────────────────────────────────────────────────────
L4  زمن التشغيل              gtp-runtime-tokio   ← موقع N-1، N-2
    ─────────────────────────────────────────────────────────────
L3  المحور                   gtp-core (connection.rs · state.rs · control/)
    ─────────────────────────────────────────────────────────────
L2  الخدمات                  gtp-recovery · gtp-cc · gtp-scheduler · gtp-path · gtp-crypto
    ─────────────────────────────────────────────────────────────
L1  السلك                    gtp-wire (header · frame · codec · varint)
    ─────────────────────────────────────────────────────────────
L0  الأنواع / الإدخال        gtp-types · gtp-io (ميتة — تحقّق §2.3)
    ─────────────────────────────────────────────────────────────
    جانبي                    gtp-sim (محاكاة حتمية)
```

**الملاحظة البنيوية المركزية:** `gtp-core` محور مطلق — كل انتقال بين الطبقات يمرّ عبر دالّتين فقط: `produce_outgoing_datagram` و`handle_incoming_datagram`. هذا **قوة** (كل درزة قابلة للتتبّع) و**خطر** (خطوة واحدة خارج ترتيبها تكسر التكامل).

**لكن الخطر الأكبر ليس هنا.** طبقة زمن التشغيل L4 **تقع خارج هذا المحور**: تفتّش الحمولة قبل التوجيه، وتدير التزامن خارج سيطرة المحرك. **أخطر درزتين في النظام (N-1، N-2) تعيشان في الطبقة الوحيدة التي لم يشملها انضباط المحور.**

### 2.2 مسارا TX/RX — الطبقة المالكة لكل خطوة

**مسار الإرسال (14 خطوة):**

| # | الخطوة | الطبقة المالكة | الحالة المقروءة عبر الحدود |
|---|---|---|---|
| 1 | بوابة الحالة (Closed/Draining) | L3 | — |
| 2 | فحص PTO + صرف السجلات | L2 recovery → L2 cc | `inflight_bytes`, `pto_duration` |
| 3 | تقليم المنتهي | L2 scheduler | `now` |
| 4 | تحديث Pacing | L2 cc | `pacing_rate` |
| 5 | `send_budget = min(cwnd − inflight, tokens)` | L3 | **`cwnd` (cc) + `inflight` (recovery)** ← درزة |
| 6 | بناء ترويسة قصيرة + KEY_PHASE | L1 + L2 crypto | `key_phase`, **`timestamp_micros`** |
| 7 | استطلاع ACK وكبتها مع موجَّه | L2 recovery + L2 path | `active_path` |
| 8 | تصريف إطارات التحكم | L3 | `control_queue` |
| 9 | سحب وترميز البيانات | L2 scheduler → L1 | `send_budget` |
| 10 | `finish` + حدّ u16 | L1 | — |
| 11 | ختم AEAD | L2 crypto | **`connection_id` + `packet_number` (اشتقاق nonce)** ← درزة |
| 12 | مقاومة التضخيم | L2 path | `bytes_received` |
| 13 | تسجيل الإرسال | L2 recovery + L2 cc | `inflight_bytes` |
| 14 | زيادة رقم الحزمة | L3 | `packet_number` |

**مسار الاستقبال (12 خطوة):**

| # | الخطوة | الطبقة المالكة | ملاحظة الدرزة |
|---|---|---|---|
| 1 | عدّ البايتات | L2 path | ⚠️ **قبل المصادقة** — انتهاك INV-3 |
| 2 | مضاد التضخيم | L2 path | — |
| 3 | فكّ الترويسة | L1 | ⚠️ **`timestamp_micros` يُفكّ ثم يُهمَل** |
| 4 | مطابقة CID | L3 | — |
| 5 | نافذة الإعادة (**check** فقط) | L2 crypto | فصل check/commit — INV-9 ✅ |
| 6 | فكّ التعمية ببتة الطور | L2 crypto | AAD = الترويسة كاملة |
| 7 | نافذة الإعادة (**commit**) | L2 crypto | بعد المصادقة حصراً ✅ |
| 8 | تكرير الإطارات | L1 | ⚠️ نوع مجهول = خطأ قاتل |
| 9 | التوزيع حسب الصنف | L3 → L2 scheduler | — |
| 10 | تحديث متتبّع ACK | L2 recovery | **عيّنة RTT ← درزة مركزية** |
| 11 | فحص الضغط الخلفي | L2 cc | **يقرأ `min_rtt` من recovery** ← درزة X-1 |
| 12 | معالجة الإغلاق | L3 | — |

> **لا خطوة في مسار RX تستهلك `timestamp_micros`.** هذه أهم ملاحظة في الخريطة كلها.

### 2.3 جدول التحقّق المباشر من `1c0e488`

كل بند أدناه مُثبت بقراءة مباشرة من الشجرة — لا افتراضات.

| # | البند المتحقَّق منه | النتيجة | الدليل |
|---|---|---|---|
| V-1 | **`timestamp_micros` مستهلَك في RX؟** | ❌ **لا — صفر مستهلك** | يظهر حصراً في `gtp-wire/src/header.rs` (حقل/بناء/ترميز/فك) و`gtp-cli/src/main.rs:108` (طباعة تشخيصية). **لا وجود له في `gtp-core` ولا `gtp-recovery` ولا `gtp-cc`** |
| V-2 | **AAD يغطي الترويسة كاملة؟** | ✅ **نعم — يشمل الطابع الزمني** | `connection.rs:311` `split_at_mut(header_consumed)` · `:918` `split_at_mut(header_len)` · `:922` `&aad_slice[..header_len]` |
| V-3 | **اشتقاق nonce يستخدم CID كاملاً؟** | ❌ **لا — أول 4 بايتات فقط** | `gtp-crypto/src/aead.rs`: `for i in 0..4 { nonce[i] ^= cid_bytes[i]; }` — والتعليق فوقها يقول «CID ‖ full 64-bit PacketNumber» وهو **وصف غير دقيق داخل الكود نفسه** |
| V-4 | **`min_rtt` يُعاد ضبطه عند الهجرة؟** | ❌ **لا — لا مسار إعادة ضبط إطلاقاً** | `gtp-recovery/src/rtt.rs:23` تهيئة بـ`u64::MAX`، `:40` `self.min_rtt.min(raw_rtt)` — **لا `reset` ولا خطّاف هجرة في الشجرة كلها** |
| V-5 | **من يقرأ `min_rtt`؟** | ⚠️ **حساب الضغط الخلفي** | `gtp-cc/src/backpressure.rs:23-24`: `rtt_inflation = current_rtt / min_rtt` — يؤكد الأثر المتوقَّع بالضبط |
| V-6 | **إطار مجهول = خطأ قاتل؟** | ✅ **نعم — قائم** | `gtp-wire/src/frame.rs:709` `_unknown => Err(TransportError::MalformedFrame("Unknown TLV frame type"))` |
| V-7 | **`HandshakeFinish` بترويسة طويلة؟** | ✅ **نعم — إصلاح N-1 آمن** | `endpoint.rs:171` `PacketHeader::new_long(1, cid, PacketNumber(1), 0, 0)` — وكذلك `:113` و`:414`. **الأطر الثلاثة كلها long ⟹ بوابة `is_long_header()` لا تكسر المصافحة** |
| V-8 | **`gtp-io` مستخدمة فعلاً؟** | ❌ **لا — ميتة** | تبعية معلنة في `Cargo.toml` لكن `endpoint.rs:14` `use tokio::net::UdpSocket;` و`:39` `socket: Arc<UdpSocket>` — الوصول المباشر يتجاوزها |
| V-9 | **DRR بمؤشّر جولة دائم؟** | ❌ **لا — يعاد من P1 دائماً** | `gtp-scheduler/src/scheduler.rs`: `for _ in 0..tiers.len() { for &tier in &tiers { … } }` مع `deficits[idx] += weight*100` عند **كل زيارة**، و`return Some(front)` يخرج من P1 فوراً |
| V-10 | **PTO يستدعي `on_timeout`؟** | ✅ **نعم — N-4 قائم** | `connection.rs:722-723` `self.hot.loss_detector.on_timeout(now); self.hot.cc.on_timeout(now);` |
| V-11 | **ECN مُمرَّر في زمن التشغيل؟** | ❌ **لا — صفر ذكر** | `grep -rn "ecn" crates/gtp-runtime-tokio/src/` ⟹ لا نتائج. **مسار ECN ميت من طرف إلى طرف** |

> **أثر عملي مباشر:** V-7 يرفع مخاطرة الانحدار عن إصلاح N-1 — يمكن دمجه فوراً. وV-1/V-2 معاً يفتحان الباب الذي تعتمد عليه كل مواصفة القياس في §7.

---

## 3. تحليل التكامل بين الطبقات

هذا القسم هو جوهر الورقة. الفرضية العاملة:

> **العيوب الحرجة في نظام طبقي لا تسكن الطبقات — تسكن الدرزات. وكل عيب حرج في GTP-rs حتى اليوم أثبت هذه الفرضية.**

### 3.1 جرد الحالة المشتركة

كل قيمة تعبر حدّ طبقة على الأقل مرة واحدة:

| الحالة | المالك | الكُتّاب | القُرّاء عبر الحدود | الثابت الحاكم | الوضع |
|---|---|---|---|---|---|
| `inflight_bytes` | L2 recovery (`loss_detector`) | recovery فقط | L3 (ميزانية الإرسال)، L2 cc | INV-1 | ✅ FR-3 |
| `smoothed_rtt` / `rttvar` | L2 recovery (`RttStats`) | recovery | L2 cc (مزامنة)، L2 scheduler (مهلة)، L3 (مقاييس) | INV-2 | ✅ FR-4 |
| **`min_rtt`** | L2 recovery | recovery (min أحادي الاتجاه) | **L2 cc/backpressure** | **INV-2b** | ❌ **X-1** |
| `cwnd` / `ssthresh` | L2 cc | cc | L3 (ميزانية الإرسال) | INV-1 | ✅ |
| `pacing_tokens` | L2 cc | cc | L3 | — | ✅ |
| `active_path` | L3 (`hot`) | L3 عند الهجرة | L2 path، L2 recovery (كبت ACK) | INV-11 | ✅ FR-4 |
| `packet_number` | L3 (`hot`) | L3 | **L2 crypto (nonce)**، L2 recovery، L1 | **INV-9** | ✅ |
| `connection_id` | L3 (`hot`) | ثابت بعد المصافحة | **L2 crypto (nonce)**، L4 (توجيه) | **INV-9، INV-4** | ⚠️ V-3 |
| `key_phase` | L2 crypto | crypto عند التدوير | L1 (بتة الترويسة)، L3 | INV-10 | ✅ R-8 |
| نافذة الإعادة | L2 crypto | crypto | L3 (فصل check/commit) | INV-3 | ✅ |
| عدّاد مضاد التضخيم | L2 path | **L3 قبل المصادقة** | L3 | **INV-3** | ❌ **A-5** |
| `deficits[5]` | L2 scheduler | scheduler | — | INV-6 | ❌ **N-3، X-19** |
| `total_stale_drops` | L2 scheduler | scheduler | L3 (مقاييس) | INV-17 | ⚠️ **X-15** |
| **`timestamp_micros`** | L1 wire | **L3 عند TX** | **لا أحد** | **INV-18** | ❌ **X-4** |
| عدّادات ECN | L2 cc | — (لا مصدر) | L3 (مقاييس) | INV-18 | ❌ **X-14** |
| خريطة الاتصالات | L4 runtime | L4 | L4 | INV-5 | ❌ **N-2** |

### 3.2 مصفوفة الاقتران بين الطبقات

الخلية = عدد قنوات الحالة المشتركة / التقييم.

| ↓ يقرأ من → | L1 wire | L2 rec | L2 cc | L2 sched | L2 path | L2 crypto | L3 core | L4 rt |
|---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| **L2 recovery** | 1 | — | 0 | 0 | 1 ⚠️ | 0 | 3 | 0 |
| **L2 cc** | 0 | **3 ⚠️** | — | 0 | 0 | 0 | 2 | 0 |
| **L2 scheduler** | 1 | 1 | 1 | — | 0 | 0 | 2 | 0 |
| **L2 path** | 0 | 0 | 0 | 0 | — | 0 | 2 ⚠️ | 0 |
| **L2 crypto** | 2 | 0 | 0 | 0 | 0 | — | 2 | 0 |
| **L3 core** | 4 | 4 | 3 | 3 | 2 | 3 | — | 0 |
| **L4 runtime** | **2 ❌** | 0 | 0 | 0 | 0 | 0 | 2 ❌ | — |

**قراءات المصفوفة:**

1. **`cc ← recovery` أكثف اقتران في النظام (3 قنوات)** وهو مصدر عيبين من أربعة (N-4 عبر `on_timeout`، X-1 عبر `min_rtt`). أي محرك توجيه يضيف قناة رابعة — وهذا يستوجب حذراً خاصاً.
2. **`L4 → L1` اقتران غير مشروع (❌).** طبقة زمن التشغيل تفكّ إطارات وتفتّش حمولة — قفزة فوق L3 كاملة. هذا **بالضبط** مصدر N-1. الاقتران الصحيح: `L4 → L3` عبر CID فقط.
3. **`path → core` معكوس الاتجاه (⚠️).** L3 يغذّي عدّاد L2 path ببايتات **غير مصادَقة** — انتهاك INV-3.
4. **L2 مفكوكة أفقياً بشكل جيد** عدا `cc ← recovery`. هذا مؤشر تصميم صحّي.

### 3.3 تصنيف الدرزات

| النوع | التعريف | أمثلة | خطر التكامل |
|---|---|---|---|
| **D1 — درزة قيمة** | قيمة واحدة تعبر الحدّ | `cwnd` → ميزانية الإرسال | منخفض — قابل للاختبار مباشرة |
| **D2 — درزة ترتيب** | صحّة تعتمد على ترتيب استدعاءات عبر طبقات | check/commit لنافذة الإعادة | **عالٍ** — لا يرصده اختبار وحدة |
| **D3 — درزة تفسير** | طبقة تفسّر بيانات تملكها طبقة أخرى | L4 يفكّ حمولة L1 | **حرج** — مصدر N-1 |
| **D4 — درزة تزامن** | مورد متشارك تحت قفل/انتظار | قفل الاتصال + قناة mpsc | **حرج** — مصدر N-2 |
| **D5 — درزة دلالة** | نفس القيمة بمعنى مختلف بين طبقتين | PTO كـ«مسبار» مقابل RTO كـ«فقد» | **عالٍ** — مصدر N-4 |
| **D6 — درزة نطاق** | قيمة نطاقها الصحيح ≠ نطاقها الفعلي | `min_rtt` نطاقه اتصال، ودلالته مسار | **عالٍ** — مصدر X-1 |

> **الاستنتاج المنهجي:** أربعة من العيوب الحرجة الخمسة من نوع D3–D6، وهي أنواع **لا يرصدها اختبار وحدة ولا اختبار طبقة**. الأداة الوحيدة الفعّالة ضدها هي **اختبار ثابت عابر للطبقات** — وهو ما يُبنى في §9.

### 3.4 ثوابت التكامل

كل ثابت: بيان + الطبقات المعنيّة + الحالة + الاختبار المطلوب.

#### مجموعة أ — ثوابت الحالة

**INV-1 — مصدر وحيد للحقيقة**
> لكل كمية مشتركة عدّاد واحد؛ يُمنع نسخها أو نسخها المرآتي في طبقة أخرى.
> الطبقات: recovery ↔ cc ↔ core · **الحالة: ✅ مغلق بـFR-3** (مرآة CUBIC أُزيلت، `debug_assert` يكشف الانحراف).

**INV-2 — وحدة مصدر RTT**
> `cc` و`scheduler` والمقاييس تقرأ RTT من `RttStats` حصراً، ومزامنة `cc.on_rtt()` تسبق تحديث النافذة.
> **الحالة: ✅ مغلق بـFR-4.**

**INV-2b — صلاحية `min_rtt` للمسار الحالي** 🔴
> `min_rtt` يجب أن يمثّل المسار النشط الحالي، ويُعاد ضبطه عند كل تغيير مؤكَّد للمسار (RFC 9002 §5.2).
> **الحالة: ❌ منتهَك — لا مسار إعادة ضبط (V-4). الأثر مؤكَّد عبر `backpressure.rs:23-24` (V-5).**

**INV-11 — نطاق المقياس مطابق لنطاق القرار** 🔴
> أي مقياس يُستخدم لقرار توجيه يجب أن يكون منسوباً للمسار الذي أنتجه.
> **الحالة: ❌ منتهَك بنيوياً** — `RttStats` نطاقها الاتصال، وقرارات المحرك نطاقها المسار. هذا **الثابت الجذري** الذي تتفرّع عنه X-1 وX-5 وX-17.

#### مجموعة ب — ثوابت الأمان والصحّة

**INV-3 — لا تحوير حالة قبل المصادقة**
> لا يجوز لمدخل غير مصادَق أن يحوّر أي حالة دائمة.
> **الحالة: ⚠️ منتهَك جزئياً** — نافذة الإعادة سليمة (check/commit)، لكن `on_bytes_received` لمضاد التضخيم يُغذّى قبل المصادقة (A-5). أثر محدود لكنه يرفع سقف التضخيم بحركة مزوّرة.

**INV-4 — التوجيه يعتمد على حقول مصادَقة حصراً** 🔴
> أي قرار توجيه/توزيع يستند إلى حقول ترويسة داخل AAD فقط — **لا يُفتَّش نصّ التعمية أبداً**.
> **الحالة: ❌ منتهَك = N-1.** طبقة L4 تفتّش أول بايت من نصّ التعمية وتقارنه بأنواع إطارات المصافحة ⟹ إسقاط 3/256 = **1.172%**.
> **الإصلاح المتحقَّق من سلامته:** بوابة `header.flags.is_long_header()` — آمنة لأن الأطر الثلاثة كلها long (V-7).

**INV-9 — تفرّد الـnonce**
> الزوج (مفتاح، nonce) لا يتكرر أبداً.
> **الحالة: ✅ قائم — لكن مبرّره الحقيقي غير موثّق.** التفرّد يأتي من `PacketNumber` **وحده** تحت المفتاح الواحد؛ الـCID **لا يضيف أماناً** وتُستخدم منه 32 بتة فقط (V-3).
> **الثابت المشتق الواجب توثيقه صراحةً:** **«مفتاح واحد ⟺ اتصال واحد»**. أي مشاركة مفتاح بين اتصالين يتشاركان أعلى 32 بتة من CID تُنتج **إعادة استخدام nonce** وانهياراً كاملاً لـChaCha20-Poly1305. هذا الثابت غير مكتوب في أي وثيقة، وهو أول ما يكسره أي تصميم بوّابة/مُرحِّل لاحق.

**INV-10 — AAD يغطي كل حقل مؤثر في السلوك**
> أي حقل ترويسة يؤثر في قرار أو قياس يجب أن يكون داخل AAD.
> **الحالة: ✅ في الكود (V-2) — ❌ في المواصفة.** `GTP-SEC-01` يعدّد AAD تعداداً ناقصاً (Flags, Version, HdrLen, CID, PN) بينما الكود يغطي الترويسة كاملة. **تعارض spec/impl حرج** لأن كل مواصفة القياس في §7 تعتمد على أن `timestamp_micros` موثَّق (X-2).

#### مجموعة ج — ثوابت الحيوية والتقدّم

**INV-5 — لا حجب تحت قفل مشترك** 🔴
> يُمنع الانتظار على قناة محدودة أثناء الإمساك بقفل يتشاركه أكثر من اتصال.
> **الحالة: ❌ منتهَك = N-2.** الأثر الحقيقي **نطاق الانفجار** (تجميد كل الاتصالات)، لا نسبة الـ37.85% المقيسة على اتصال واحد (X-13).

**INV-6 — تقدّم وإنصاف المجدول** 🔴
> كل طبقة غير فارغة تُخدَم خلال عدد محدود من استدعاءات `pop_next`.
> **الحالة: ❌ منتهَك = N-3.** مؤكَّد بالكود (V-9): الحلقة تعود من P1 عند كل استدعاء، والعجز يتراكم عند كل زيارة.

**INV-8 — حيوية التسليم المرتّب**
> لا يجوز لمخزن إعادة الترتيب أن يتوقف إلى ما لا نهاية بانتظار فجوة.
> **الحالة: ❌ منتهَك = FR-5.** إثبات بنيوي قاطع: `OrderedGroupReceiver::on_incoming` **لا تستقبل معطى زمن أصلاً**، فلا يمكن لمهلة أن تنتهي.

#### مجموعة د — ثوابت الدلالة

**INV-7 — صلاحية إشارة الازدحام** 🔴
> انهيار النافذة يُشترط بدليل ازدحام **مستمر**، لا بانتهاء مهلة مسبار.
> **الحالة: ❌ منتهَك = N-4** (V-10). RFC 9002 §7.5 يميّز صراحةً: مسبار PTO ليس دليل فقد.

**INV-17 — أفضلية المقياس الأقرب لتجربة المستخدم**
> عند توفّر مقياس يقيس التجربة مباشرة، لا يُستعاض عنه بوكيل.
> **الحالة: ⚠️ غير مطبَّق.** `total_stale_drops` (نسبة رسائل اللعب التي وصلت بلا فائدة) موجود ومعروض، وغائب كلياً عن نموذج التسجيل M4 (X-15).

**INV-18 — لا سطح ميت في مسار حرج**
> كل حقل يُرسَل على السلك أو يُعرَض في المقاييس له مستهلك حقيقي، أو يُحذف.
> **الحالة: ❌ منتهَك مرتين حرجتين:** `timestamp_micros` (X-4) وعدّادات ECN (X-14). ومنتهَك بشكل غير حرج في: `VarInt`، `gtp-io`، `packets_since_ratchet`، `deficits[0]`، `supersedable`، `deadline_ms`، حقول التجزئة.

#### مجموعة هـ — ثوابت جديدة يفرضها المحرك

**INV-13 — وسم الحقبة**
> كل عيّنة قياس تحمل معرّف الحقبة/المسار الذي أُخذت عليه؛ العيّنات عبر الحقب لا تُخلَط أبداً.

**INV-14 — استقلال القياس عن القرار**
> آلية القياس يجب ألا تُشوّش الكمية التي تقيسها، ولا تلوّث حالة CC للمسار الحامل للبيانات.

**INV-15 — الفشل الآمن**
> تعطيل المحرك أو فشله يعيد **سلوك الأساس بالضبط** — يُثبَت باختبار سلبي.

**INV-16 — زمن قرار محدود ومقاس**
> `detect + confirm + switch` محدود بسقف معلن ومُقاس، لكل فئة قرار على حدة.

**INV-19 — استقرار عقد الـAPI تحت فعل مستوى التحكم**
> قرار في مستوى التحكم (توجيه، MTU) لا يجوز أن يغيّر عقداً مرئياً للتطبيق أثناء الجلسة.
> **الخطر:** مع **انعدام التجزئة** (حقول `total_fragments`/`fragment_id` دكور)، تخفيض MTU يبدأ برفض رسائل كان التطبيق يرسلها بنجاح (X-10).

**INV-20 — أمانة المسار المقاس**
> المسار الذي يُقاس يجب أن يكون المسار الذي ستسلكه البيانات.
> **الخطر البنيوي:** ECMP يُجزّئ على الـ5-tuple. أي قياس خارج النطاق (اتصال/منفذ مصدر مختلف) قد يعبر مساراً فيزيائياً مختلفاً عن التدفّق الحقيقي (X-17). **هذا الثابت غير قابل للضمان مسبقاً — يُعالَج بالتحقق البعدي (RE-7).**

### 3.5 مصفوفة العيوب ← الثوابت

| العيب | الثابت المنتهَك | نوع الدرزة | الطبقات | يرصده اختبار وحدة؟ |
|---|---|:---:|---|:---:|
| N-1 | INV-4 | **D3 تفسير** | L4 ✗ L1 | ❌ لا |
| N-2 | INV-5 | **D4 تزامن** | L4 ✗ L3 | ❌ لا |
| N-3 | INV-6 | D1/D2 | L2 sched | ⚠️ جزئياً |
| N-4 | INV-7 | **D5 دلالة** | L2 rec ✗ L2 cc | ❌ لا |
| X-1 | INV-2b + INV-11 | **D6 نطاق** | L2 rec ✗ L2 cc | ❌ لا |
| A-5 | INV-3 | D2 ترتيب | L3 ✗ L2 path | ❌ لا |
| FR-5 | INV-8 | D2 ترتيب | L3 ✗ L2 sched | ⚠️ جزئياً |
| X-4 | INV-18 | D1 | L1 ✗ L3 | ❌ لا |
| X-14 | INV-18 | D1 | L4 ✗ L2 cc | ❌ لا |
| X-15 | INV-17 | D1 | L2 sched ✗ محرك | ❌ لا |
| X-19 | INV-6 | D1 | L2 sched | ❌ لا |

> **النتيجة الحاكمة: 9 من 11 عيباً لا يرصدها أي اختبار وحدة أو طبقة.** وهذا يفسّر تماماً كيف اجتازت الشجرة 91/0 اختباراً أخضر وclippy نظيفاً بينما تُسقط 1.17% من حركتها صامتةً. **البوابة الخضراء ليست دليل سلامة تكامل — إنها دليل سلامة طبقات.**

---

## 4. إطار الحوكمة المشتق

من §3 نستخلص أربع قواعد ملزِمة لكل عمل لاحق:

| القاعدة | البيان |
|---|---|
| **G-R1** | كل ثابت في §3.4 يملك **اختباراً مسمّى** يفشل عند انتهاكه — وإلا يُعتبر الثابت غير مطبَّق |
| **G-R2** | كل PR يمسّ درزة من نوع D2–D6 يُرفق **تحليل أثر** يحدّد الثوابت المتأثرة |
| **G-R3** | أي مقياس يدخل دالة قرار يجب أن **يمرّ باختبار نطاق** (INV-11): هل نطاقه مطابق لنطاق القرار؟ |
| **G-R4** | **بند بلا اختبار = غير منفَّذ** (موروثة، مؤكَّدة) |

---

## 5. التدقيق المعمّق — النتائج الجديدة

### 5.1 النتائج الجديدة X-1 … X-19

| # | النتيجة | الخطورة | الحالة |
|---|---|:---:|:---:|
| **X-1** | `min_rtt` لا يُعاد ضبطه عند الهجرة ⟹ الضغط الخلفي مرفوع دائماً بعد أي هجرة | **P0** | مؤكَّد V-4/V-5 |
| **X-2** | تعارض spec/impl في AAD: `GTP-SEC-01` يعدّد ناقصاً، الكود يغطي الترويسة كاملة | **P0** | مؤكَّد V-2 |
| **X-3** | وصف الـnonce غير دقيق (CID[0..4] لا CID كاملاً) + ثابت «مفتاح ⟺ اتصال» غير موثّق | P1 | مؤكَّد V-3 |
| **X-4** | `timestamp_micros` حقل ميت — أثمن أصل مهدور في المشروع | **P1** | مؤكَّد V-1 |
| **X-5** | استقصاء 1Hz **لا يستطيع** قياس jitter — خطأ فئوي في نموذج القياس | **P0** (للمحرك) | تحليلي |
| **X-6** | خلط زمن القرار (≈4s) بزمن التبديل (43ms) ⟹ ادعاء تحقيق عتبة 500ms مقلوب | **P0** (للمحرك) | تحليلي |
| **X-7** | التسجيل بالشرائح يتصارع مع منع التذبذب — التذبذب من النموذج لا الشبكة | P1 | تحليلي |
| **X-8** | خانة تحدٍّ واحدة + مهلة 3s + لا إعادة إرسال + استبدال صامت ⟹ **T3 شرط لا خيار** | **P0** (للمحرك) | مؤكَّد بالتدقيق |
| **X-9** | غموض دلالي في T1: «إعادة ضبط» تحتمل معنيين متعاكسي الأثر | P1 | تحليلي |
| **X-10** | تخفيض MTU + انعدام التجزئة ⟹ فشل `PayloadTooLarge` مرئي للتطبيق (INV-19) | P1 | تحليلي |
| **X-11** | مقام الحصص العادلة في مجسّ N-3 خاطئ (85 بدل 55) ⟹ تقليل حجم الظلم بالنصف | P2 | حسابي |
| **X-12** | فقد WAN 0.40% **تحت** أرضية N-1 البالغة 1.17% ⟹ نموذج الأدلة لا يُنتجها | **P1** | حسابي |
| **X-13** | دليل N-2 (37.85%) لا يُثبت نطاق الانفجار المُدّعى | P2 | منهجي |
| **X-14** | ECN ميت من طرف إلى طرف ⟹ محور الازدحام في التسجيل أعمى | P1 | مؤكَّد V-11 |
| **X-15** | `total_stale_drops` — أقرب مقياس لتجربة اللاعب — غائب عن نموذج M4 | **P1** | تحليلي |
| **X-16** | مخاطرة انحدار مفترضة في إصلاح N-1 | — | **مُنتفية** V-7 |
| **X-17** | ECMP يُبطل أمانة القياس خارج النطاق ⟹ يلزم تحقّق بعدي | **P1** | تحليلي |
| **X-18** | 10 مواصفات معيارية في `docs/specs/` خارج نطاق تدقيق التوثيق | P2 | نطاقي |
| **X-19** | **تراكم عجز غير محدود** في DRR عندما يكون `send_budget` هو القيد لا العجز | P2 | مؤكَّد V-9 |

### 5.2 تفصيل النتائج المفصلية

#### X-1 — انقلاب `min_rtt` من «منخفض» إلى سدّي 🔴

التدقيق السابق صنّفه: *«[منخفض] لا نافذة زمنية لـ min_rtt»*. مؤكَّد من الكود أن الأثر أوسع:

```
backpressure.rs:23-24 →  rtt_inflation = current_rtt / min_rtt

هجرة من مسار 42ms إلى مسار 60ms:
    min_rtt يبقى 42ms إلى الأبد (V-4: لا مسار إعادة ضبط)
    ⟹ rtt_inflation = 60/42 = 1.43 دائماً
    ⟹ الضغط الخلفي مرفوع طوال الجلسة
    ⟹ محرك اللعبة يخفّض مستوى التفاصيل استجابةً لعطل قياس
```

**ثلاث خصائص تجعله الأخطر:** صامت، دائم، ويقع **حصراً على المسار الذي اختاره المحرك بوصفه الأفضل** — أي أن المحرك كلما نجح، عاقب نفسه.

وهو انحراف عن **RFC 9002 §5.2** الذي يُلزم بإعادة ضبط `min_rtt` عند تأكيد تغيّر المسار — بند امتثال قائم، لا تحسيناً.

> **الإجراء:** توسيع N-5 وترقيته إلى **P0**: (أ) حارس `Option<Duration>` بدل `u64::MAX`؛ (ب) **`RttStats::reset_for_new_path()` تُستدعى من مسار `PathMigrated`**؛ (ج) اختبار: هجرة إلى مسار أبطأ ⟹ `min_rtt` يتبع المسار الجديد و`rtt_inflation` يعود إلى ~1.0.

#### X-8 — خانة التحدّي الواحدة تُبطل نموذج الفحص 🔴

ثلاث حقائق مؤكَّدة تتضافر:

1. مهلة التحدي **3 ثوانٍ ثابتة** بلا مؤشّر انتهاء
2. **لا إعادة إرسال** للتحدي الضائع
3. **تحدٍّ ثانٍ يستبدل الأول بصمت**

الأثر على محرك يفحص P مرشحاً بالتسلسل:

```
مسح نظيف         ≈ P × RTT              (≈ 250ms لـP=5)
مسح بفقد تحدٍّ واحد  ≈ 3s + (P−1) × RTT     (≈ 3.2s)
أسوأ حالة          ≈ 6s+
```

والبند (3) أخطر من الاثنين: أي جدولة غير منضبطة **تُلغي مسبار المرشّح السابق بصمت** فتُنتج درجات لمسارات لم تُقس — **وهو أسوأ من عدم القياس**، لأنه يُنتج ثقة زائفة.

> **الحل الموصى به ليس T3 بل RE-2 (§7.2): اتصال قياس منفصل لكل مرشّح.** يحلّ الثلاثة معاً، **ويحلّ INV-11 (نطاق المقياس) بنيوياً** — لأن كل اتصال قياس يملك `RttStats` خاصة به. وهو الحلّ الوحيد الذي يحافظ على «صفر تعديل نواة» حقاً.

#### X-12 — تناقض في سلسلة الأدلة 🔴

التقرير يقرّر أن إسقاط N-1 **ثابت عند 1.172%** (احتمال أول بايت تعمية ∈ {0x0B,0x0C,0x0D}، بتوزيع منتظم). ثم يذكر فقد WAN بمدى **0.40–1.94%**.

**0.40% أقل من أرضية 1.172%، ونموذج «إسقاط ذاتي ثابت + فقد إنترنت موجب» لا يمكنه إنتاج قيمة تحت الأرضية.**

وفي §4.8: *«متوسط ≈0.84% يطابق تماماً 1.17%»* — فارق نسبي **28%**، ووصف «يطابق تماماً» غير مبرَّر.

**لكن الاستدلال الأصلي صامد** — تحققتُ منه:

```
مجمّع المجسّ: 9000 مرسلة، 97 مفقودة  ⟹  1.078%
تحت H₀: p = 3/256 = 1.172%
   المتوقع = 105.5 ،  σ = √(9000·p·(1−p)) = 10.2
   z = (97 − 105.5)/10.2 = −0.83     ⟹ متسق تماماً
```

**الاستنتاج: N-1 صحيح إحصائياً ويستحق P0. المطلوب هو تفسير قراءات WAN، لا مراجعة N-1.** الفرضيات المرشّحة: (أ) جولات WAN قاست أصنافاً موثوقة تُخفي فيها إعادة الإرسال الفقد؛ (ب) عدّ مختلف بين المجسّ وجولات WAN. **يجب الحسم قبل إعادة استخدام رقم 1.17% في أي إعادة تفسير لاحقة.**

> ملاحظة دقة: النسبة المجمّعة الصحيحة **1.078%**، لا 1.10% (المتوسط غير الموزون للنسب الثلاث).

#### X-19 — تراكم عجز غير محدود في DRR (جديد)

بقراءة `scheduler.rs` (V-9): العجز يُضاف عند **كل زيارة** لطبقة غير فارغة. وعندما يكون `send_budget` — لا العجز — هو القيد:

```rust
if size <= send_budget && self.deficits[idx] >= size { … }
else { self.queues[idx].push_front(front); break; }   // العجز يبقى متراكماً
```

فالعنصر يُعاد والعجز **يبقى ويتراكم عند الاستدعاء التالي**. النتيجة: طبقة محجوبة بميزانية الإرسال تُراكم عجزاً بلا سقف، ثم **تُطلق دفعة ضخمة** لحظة اتساع الميزانية — وهو نقيض غرض الـpacing.

> **الإصلاح مع FR-7:** سقف للعجز عند `quantum × k` (مثلاً k=2)، وإضافة العجز **مرة واحدة لكل جولة** عند مؤشّر الجولة الدائم، لا عند كل استدعاء.

### 5.3 إعادة تصنيف الخطورة تحت تشغيل المحرك

**هذا الجدول هو أهم مخرَج تشغيلي في الورقة.**

| البند | التصنيف السابق | **التصنيف تحت المحرك** | سبب الانقلاب |
|---|:---:|:---:|---|
| **X-1 / N-5** `min_rtt` | منخفض (عرض) | 🔴 **P0 سدّي** | الهجرة المتكررة تجعل العطل دائماً؛ الضغط الخلفي يفسد كل قياس لاحق |
| **T3** خانات تحدٍّ | اختياري | 🔴 **P0 شرط** | خانة واحدة + 3s + لا إعادة إرسال ⟹ الفحص المتسلسل غير قابل للتنفيذ (X-8) |
| **T1** مضاد التضخيم | «فائدة مستقلة» | 🟠 **P1 بدلالة محدَّدة** | التفسير الخاطئ يُنتج انهيار إنتاجية دورياً (X-9) |
| **N-1** إسقاط 1.17% | P0 | 🔴 **P0 مضاعف** | يلوّث كل درجة مسار بضوضاء 1.17% ⟹ قرارات المحرك على مدخلات فاسدة |
| **N-4** PTO=RTO | P1 | 🟠 **P1 مضاعف** | مسبار كاذب على مسار جديد ينهار بالنافذة فيبدو المسار سيئاً ⟹ **حلقة تغذية راجعة موجبة نحو التذبذب** |
| **X-14** ECN ميت | غير مصنّف | 🟠 **P1** | محور الازدحام في التسجيل أعمى |
| **X-10** MTU/التجزئة | غير مصنّف | 🟠 **P1** | قرار توجيه يكسر عقد API (INV-19) |

> **حلقة N-4 تستحق وقفة:** المحرك يهاجر إلى مسار جديد ⟹ `RttStats` تحمل تقدير المسار القديم ⟹ PTO مبكر كاذب ⟹ `cc.on_timeout` ينهار بـcwnd إلى `2×SMSS` ⟹ الإنتاجية تنهار على المسار الجديد ⟹ المحرك يقرأ ذلك تدهوراً ⟹ يهاجر عائداً. **هذه حلقة تذبذب مُولَّدة ذاتياً لا تمنعها آلية hysteresis إطلاقاً، لأن التدهور المقيس حقيقي — سببه فقط داخلي.** إصلاح N-4 وX-1 معاً شرط لأي منع تذبذب فعّال.

---

## 6. المعمارية المستهدفة

### 6.1 المبدأ الحاكم

> **القرار خارج النواة · التنفيذ عبر واجهات قائمة · الحالة خارج الاتصال · القياس بنطاق مطابق لنطاق القرار.**

الشقّ الرابع إضافة هذه الورقة، وهو ما يترجم INV-11 إلى قرار معماري.

### 6.2 مخطط المكوّنات

```
╔═══════════════════════ خارج النواة — لا يمسّ المسار الساخن ═══════════════════════╗
║                                                                                    ║
║   ┌────────────────────────── gtp-route (نقي) ──────────────────────────────┐      ║
║   │  بلا tokio · بلا gtp-core · ساعة محقونة · حتمي بالكامل                   │      ║
║   │                                                                          │      ║
║   │   PathStats<P>     rtt · owd_var · jitter · loss · stale · epoch         │      ║
║   │   Scorer           تطبيع مستمر → درجة [0,1]              (RE-4)          │      ║
║   │   DecisionFSM      STEADY→LEADING→CONFIRM→SWITCH→VERIFY   (RE-5)         │      ║
║   │   FlapSuppressor   hysteresis · hold · cooldown · budget  (RE-5)         │      ║
║   │   MtuGovernor      PLPMTUD مقيّد بالرفع فقط               (RE-8)          │      ║
║   │   PathEventDetect  مميّز إعادة التوجيه عن الازدحام        (RE-6)          │      ║
║   │   RevertGuard      تحقّق بعد التبديل + تراجع تلقائي        (RE-7)          │      ║
║   │                                                                          │      ║
║   │   trait RouteTarget { probe · results · metrics · events · apply }        │      ║
║   └──────────────────────────────────▲───────────────────────────────────────┘      ║
║                                      │ ينفّذها                                      ║
║   ┌──────────────────── gtp-route-tokio (مهايئ رقيق) ───────────────────────┐      ║
║   │  اتصالات قياس منفصلة لكل مرشّح            (RE-2 — يحلّ INV-11 وX-8)      │      ║
║   │  استقصاء DetailedMetrics + drain_events @ 1Hz                            │      ║
║   │  تنفيذ القرار: هجرة · AckFrequency · سقف MTU                             │      ║
║   │  وضع الظل (مراقبة بلا هجرة) · مفتاح تعطيل يعيد الأساس بالضبط (INV-15)    │      ║
║   └──────────────────────────────────────────────────────────────────────────┘      ║
╚════════════════════════════════════════════════════════════════════════════════════╝
                                       │ يستهلك فقط
╔══════════════════════════════ النواة — تغييرات محدودة ══════════════════════════════╗
║  C-1  استهلاك timestamp_micros في RX → owd_var           (RE-1) ~30 سطراً           ║
║  C-2  RttStats::reset_for_new_path() على PathMigrated     (X-1)  ~10 أسطر           ║
║  C-3  خانات تحدٍّ متعددة أو إلغاء صريح                      (X-8)  ~40 سطراً           ║
║  C-4  ControlEvent: OwdSample · PathEventDetected         (RE-1/6) ~20 سطراً        ║
╚════════════════════════════════════════════════════════════════════════════════════╝
```

### 6.3 تصحيح جوهري: «صفر تعديل نواة» غير قابل للتحقيق

دراسة المحرك تعلن *«لمسات النواة المطلوبة: صفر إلزامية»*. **هذا صحيح فقط لوضع الظل بمرشّح واحد.** للمحرك الكامل (M1–M6 + M8) يلزم أربعة تعديلات نواة (C-1…C-4) بإجمالي **~100 سطر**.

**لكن القيد الحاكم لا يزال محقّقاً**، وبتفسير أدق:

| ما ينصّ عليه القيد | التحقيق |
|---|---|
| «بلا زيادة ملموسة في الكلفة الحوسبية» | **صفر إضافة للمسار الساخن.** C-1 خطوة قراءة `u32` + `wrapping_sub` + تحديث EWMA في RX — عمليات بأرقام صحيحة. C-2 يعمل عند الهجرة فقط. C-3/C-4 خارج المسار الساخن |
| «بلا تعقيد تشغيلي» | كل ميزة المهايئ خيار تشغيلي؛ التعطيل يعيد الأساس بالضبط (INV-15) |
| «محرك مستقل بالكامل» | ✅ `gtp-route` بلا اعتمادية على النواة إطلاقاً |
| «لا تعقيد على البنية الأساسية» | ✅ لا تغيير صيغة سلك · لا حالة جديدة في الاتصال · لا خطوة جديدة في TX |

> **والأهم: C-1 وC-2 ليسا كلفة — إنهما إصلاحان قائمان بذاتيهما.** C-2 امتثال RFC 9002 §5.2، وC-1 تفعيل حقل مُرسَل أصلاً (إغلاق INV-18). أي أن **الكلفة الصافية للنواة سالبة**.

### 6.4 قرار معماري: لا تعدّد مسارات داخل الاتصال

`GTP-ARCH-01` ينصّ: *«One connection owns one active path, one congestion controller, one pacing engine.»*

كسر هذا الثابت يستوجب إعادة كتابة `gtp-cc` و`gtp-recovery` و`gtp-path` و`gtp-core` — أي **إبطال تدقيق الـ516 قسماً والـ91 اختباراً**. المقابل غير مبرَّر.

**البديل المعتمد — فصل مستوى البيانات عن مستوى القياس:**

| المستوى | البنية | المكسب |
|---|---|---|
| **البيانات** | اتصال GTP واحد · PN space واحد · CC واحد · مسار نشط واحد | يحافظ على كل الثوابت المدقَّقة |
| **القياس** | اتصال قياس صغير مستقل لكل مرشّح (RE-2) | **`RttStats` لكل مسار ⟹ INV-11 محقَّق بنيوياً** · صفر تلوّث لـCC البيانات (INV-14) · لا يصطدم بخانة التحدّي (X-8) |

**ثلاث فوائد غير بديهية:**
1. المشكلة الكلاسيكية في التوجيه التكيفي — *أن القياس يفسد ما يقيسه* — تختفي بنيوياً.
2. `PING { nonce: u64 }` الموجود يحمل `probe_seq` ⟹ **لا حاجة لإطار جديد إطلاقاً**.
3. كل اتصال قياس يملك CC خاصاً — وهو **السلوك الصحيح** أصلاً: مسارات مختلفة يجب ألا تتقاسم حالة ازدحام.

**والكلفة:** P اتصالاً إضافياً (مصافحة + مفاتيح + مدخل في خريطة الاتصالات). لـP ≤ 4 مقبولة تماماً. **وشرط أمني ملزِم:** كل اتصال قياس يملك مفتاحه الخاص — حفاظاً على INV-9 («مفتاح ⟺ اتصال»).

---

## 7. مواصفة الآليات المقترحة

### RE-1 — طبقة القياس الزمني: استهلاك `timestamp_micros`

**الغرض:** استخراج تباين التأخير أحادي الاتجاه (OWD variance) وjitter حقيقي **بمعدل التِك**، بصفر بايت على السلك.

**الأساس المتحقَّق منه:** V-1 (الحقل مُرسَل وغير مستهلَك) + V-2 (داخل AAD ⟹ لا يمكن لطرف على المسار تزويره).

**الخوارزمية (تُنفَّذ بعد نجاح المصادقة حصراً — INV-3):**

```rust
// المدخل: hdr.timestamp_micros (u32) ، now (MonotonicTime)
let ts_peer  = hdr.timestamp_micros;
let ts_local = now.as_micros() as u32;
let d        = ts_local.wrapping_sub(ts_peer);   // ثابت + انزياح ساعة مجهول

// تفسير موقّع يعالج حافة الالتفاف بصحّة تامة:
let delta = d.wrapping_sub(self.base_d) as i32;  // µs

if delta < 0 { self.base_d = d; }                // قاع منزلق جديد
let owd_var = delta.max(0) as u32;               // µs — الانزياح الثابت أُلغي بالطرح

// jitter وفق RFC 3550 §6.4.1:
let diff = (owd_var as i64 - self.prev_owd_var as i64).abs();
self.jitter += (diff - self.jitter) / 16;
self.prev_owd_var = owd_var;
```

**الحدود العددية والقيود:**

| البند | القيمة | التبرير |
|---|---|---|
| التفاف `u32` | كل **71.58 دقيقة** | `2^32 µs`. لا يُشكّل مشكلة: `d` ثابت (كلا الساعتين تتقدمان بنفس المعدل)، و`wrapping_sub` + التفسير الموقّع يعالج حافة 0/2^32 لكل `|delta| < 2^31` |
| نافذة القاع المنزلق | **≤ 30 ثانية** | انحراف البلورات 10–50 ppm ⟹ 0.6–3 ms/دقيقة. نافذة 30s تُبقي خطأ الانحراف ≤ 1.5ms عند 50ppm |
| إعادة ضبط `base_d` | **إلزامي عند تغيّر الحقبة** | INV-13 — القاع منسوب للمسار |
| إعادة الإرسال | **لا تُفسد العيّنات** | الحزمة المعادة تُبنى بترويسة جديدة وطابع زمني جديد |
| الكلفة | `wrapping_sub` + مقارنة + إزاحة | صفر تخصيص · صفر عملية عائمة |

**المخرَج:** `ControlEvent::OwdSample { owd_var_us: u32, jitter_us: u32, epoch: u8 }`.

**ما يحلّه هذا وحده:**

| المشكلة | كيف |
|---|---|
| **X-5** استحالة قياس jitter عند 1Hz | jitter على المسار النشط يُقاس من **حركة اللعب الحقيقية بمعدل التِك (60Hz+)** — بصفر مسبارات |
| **M6** التوجيه غير المتماثل | تدهور اتجاه الذهاب معزول عن اتجاه العودة — بلا مزامنة ساعات (الانزياح فرقي) |
| **RE-6** مميّز إعادة التوجيه | قفزة درجية في `owd_var` هي التوقيع الأنقى لإعادة التوجيه |
| **INV-18** | يُغلق أخطر سطح ميت في النظام |

> **تصحيح لحكم في دراسة المحرك:** M6 مصنّفة *«ضمن مسبارات M2 — صفر إضافي»* بافتراض اشتقاق عدم التماثل من فرق التحدي/الصدى. **لكن التحدي/الصدى يعطي مجموع الاتجاهين لا فصلهما.** الفصل الحقيقي للاتجاهين **مستحيل بالمسبارات وحدها، وممكن بالطابع الزمني وحده.**

**⚠️ شرط مسبق ملزِم:** حسم X-2 (تعارض AAD) أولاً. مواصفة القياس كلها تفترض أن الطابع الزمني موثَّق تعميرياً. الكود يقول نعم (V-2)، والمواصفة الأمنية توحي بلا.

---

### RE-2 — اتصالات القياس المنفصلة

**الغرض:** قياس المرشحين بنطاق مطابق لنطاق القرار (INV-11) دون تلويث مستوى البيانات (INV-14) ودون الاصطدام بخانة التحدّي الواحدة (X-8).

**التصميم:**

```
تدفّق البيانات    ──►  اتصال GTP رئيسي  (CC · PN · RttStats خاصة)
                                          │ المسار النشط فقط
قياس المرشّح A    ──►  اتصال قياس A       (RttStats مستقلة تماماً)
قياس المرشّح B    ──►  اتصال قياس B
قياس المرشّح C    ──►  اتصال قياس C
```

**المعاملات:**

| المعامل | القيمة | التبرير |
|---|---|---|
| عدد المرشحين P | **≤ 4** | يحدّ الذاكرة والمصافحات؛ عائد التنويع يتناقص بعد 4 |
| نمط المسبار | **دفعة**: 8 حزم بتباعد 20ms | يعطي loss (8 عيّنات) + jitter (7 فروق) + reorder — انظر أدناه |
| دورية الدفعة | كل **10 ثوانٍ** لكل مرشّح | |
| النطاق الفعلي | 8 × 77B / 10s = **62 B/s لكل مرشّح** | 77B = 24 ترويسة + 9 إطار Ping + 16 وسم + 28 UDP/IP |
| الإجمالي عند P=4 | **~250 B/s** | أقل من 0.01% من نطاق اللعب النموذجي |

> **تصحيح عددي لدراسة المحرك:** تقدير «~40B لكل مسبار» يغفل إطار الـPing وترويسات UDP/IP. **الرقم الفعلي 77B.** لا يغيّر الحكم (الكلفة تبقى مهملة) لكنه يكرّر — بصورة مصغّرة — نفس فئة الخطأ التي تصحّحها الدراسة في الورقة المفاهيمية.

**لماذا الدفعة لا الحزمة المفردة (حلّ X-5 للمرشحين):**

| النمط | jitter؟ | loss؟ | reorder؟ | النطاق |
|---|:---:|:---:|:---:|---|
| 1 حزمة/ثانية | ❌ (تباين عبر الثواني — كمية مختلفة) | بطيء جداً | ❌ | 77 B/s |
| **8 حزم @ 20ms كل 10s** | ✅ **تشتّت حقيقي بمقياس مللي ثانية** | ✅ 8 عيّنات | ✅ | 62 B/s |

**نطاق أقل، ومعلومات أكثر بكثير.** هذا هو الفرق بين رقم يُسمّى jitter ورقم يقيسه.

---

### RE-3 — وسم الحقبة (`PathEpoch`)

**الغرض:** تحقيق INV-13 — منع خلط عيّنات القياس عبر تغيّرات المسار.

**التنفيذ:** عدّاد 3-بت في **البتات المحجوزة 2-0 من `HeaderFlags`** — صفر بايت إضافي، **وداخل AAD** (موثَّق تعميرياً).

```
Bit 7 LONG_HEADER · Bit 6 KEY_PHASE · Bit 5 ACK_PRESENT
Bits 4-3 ECN_MASK · Bits 2-0 ► PATH_EPOCH  (0–7، دائري)
```

**القواعد:**
1. يزيد عند كل هجرة مؤكَّدة.
2. أي عيّنة RTT/OWD من حقبة ≠ الحقبة الحالية **تُهمَل**.
3. عند تغيّر الحقبة: `min_rtt`، `base_d`، `jitter`، وحالة CC **تُعاد بذرتها**.

**الأثر على X-1 وN-4:** يجعل إعادة ضبط `min_rtt` **مؤتمتة وذات دلالة بروتوكولية** بدل خطّاف يدوي، ويكسر حلقة التذبذب المُولَّدة ذاتياً الموصوفة في §5.3.

**بديل بلا تغيير سلك:** حقبة محلية بحتة داخل `gtp-route`، تُطبَّق عند استدعاء الهجرة. يفقد التزامن بين الطرفين لكنه كافٍ لحماية القياس محلياً. **يُوصى بالبدء به**، والانتقال إلى بتات الترويسة عند الحاجة للتزامن.

---

### RE-4 — دالة التسجيل المستمرة

**المشكلة (X-7):** التسجيل بالشرائح (50/100/200ms) عند نقطة عمل مقيسة 42–52ms يضع النظام **على حدّ الشريحة تماماً** ⟹ تذبذب 2ms يُنتج قفزة درجة كبيرة قد تتجاوز عتبة الـ20% ⟹ **مصدر التذبذب هو النموذج الرياضي لا الشبكة**، ومنع التذبذب مصمَّم لقمع تذبذب الشبكة.

**الحل — تطبيع خطي متعدد القطع مع قصّ (بلا انقطاعات، بلا دوال متسامية):**

```rust
#[inline]
fn norm(x: f32, lo: f32, hi: f32) -> f32 {
    ((x - lo) / (hi - lo)).clamp(0.0, 1.0)     // 0 = مثالي ، 1 = أسوأ
}

fn score(p: &PathStats, w: &Weights) -> f32 {
    let s = w.rtt    * norm(p.rtt_p50_ms,    20.0, 150.0)
          + w.tail   * norm(p.rtt_p95_ms - p.rtt_p50_ms, 2.0, 60.0)
          + w.jitter * norm(p.jitter_ms,      1.0,  30.0)
          + w.loss   * norm(p.loss_pct,       0.0,   3.0)
          + w.stale  * norm(p.stale_drop_pct, 0.0,   2.0)   // ← RE-9
          + w.stab   * p.instability;                        // [0,1] مُعدّ سلفاً
    1.0 - s                                                  // أعلى = أفضل
}
```

**الأوزان الابتدائية** (Σ = 1.00 — **تُعايَر ميدانياً؛ لا توجد أوزان عالمية**):

| المحور | الوزن | التبرير |
|---|:---:|---|
| `rtt_p50` | 0.22 | أساسي لكن ليس مهيمناً |
| `rtt_p95 − p50` (الذيل) | 0.13 | **ذيل التأخير أخطر من المتوسط في الألعاب** |
| `jitter` | 0.18 | من RE-1 — قياس حقيقي لا وكيل |
| `loss` | 0.22 | |
| **`stale_drop`** | **0.15** | **RE-9 — أقرب مقياس لتجربة اللاعب** |
| `instability` | 0.10 | مشتقّ من تاريخ `jitter` وأحداث المسار |

**ملاحظتان:**
- محور «الازدحام» المستقل **محذوف عمداً**: ECN ميت (X-14)، والازدحام يظهر في RTT والذيل أصلاً. إن أُحييَ ECN لاحقاً يُضاف بوزن مقتطع من `loss`.
- `norm` **مستمرة ومقصوصة**: تذبذب 2ms عند RTT=50 يُنتج تغيّر درجة = `0.22 × 2/130 = 0.0034` — أقل بمرتبتين من عتبة الـ20%. **المشكلة تختفي رياضياً.**

---

### RE-5 — آلة حالات القرار ومنع التذبذب

```
                    ┌──────────────────────────────────────────────┐
                    │                                              │
                    ▼                                              │
              ┌──────────┐   مرشّح يتفوّق                            │
              │  STEADY  │──────────────────────►┌───────────┐     │
              └──────────┘   بهامش ≥ margin      │ CONFIRMING│     │
                    ▲                             └───────────┘     │
                    │  التفوّق زال                       │           │
                    └────────────────────────────────────┤           │
                                                         │ استمرّ    │
                                        confirm_rounds متتالية        │
                                                         ▼           │
                                                  ┌───────────┐      │
                                                  │ SWITCHING │      │
                                                  └───────────┘      │
                                                         │ PathMigrated
                                                         ▼           │
                                                  ┌───────────┐      │
                                       أفضل ──────│ VERIFYING │      │
                                       │           └───────────┘      │
                                       │                 │ أسوأ من الأساس
                                       │                 ▼           │
                                       │          ┌───────────┐      │
                                       │          │ REVERTING │──────┘
                                       │          └───────────┘  (RE-7)
                                       ▼
                                  HOLD ثم STEADY
```

**المعاملات حسب فئة القرار** (انظر RE-6 للتصنيف):

| المعامل | Failover | Degradation | Optimization |
|---|:---:|:---:|:---:|
| هامش التبديل | — (فوري) | 10% | **20%** |
| جولات التأكيد | **0** | 1 | **2** |
| نافذة التحقّق البعدي | 2s | 3s | **5s** |
| مهلة الحجز بعد التبديل | 1s | 5s | **15s** |
| سقف التبديلات/دقيقة | غير محدود | 3 | **2** |
| فترة التهدئة للمسار المطرود | 10s | 30s | **60s** |

> **تعديل على دراسة المحرك:** الدراسة تقترح hold موحّداً (30s ثم 5s معدَّلاً). **مهلة واحدة لا تصلح للفئات الثلاث**: 5s طويلة جداً لـFailover، وقصيرة جداً لـOptimization. الفصل شرط.

**ميزانية التراجع (RevertGuard):** التراجع مسموح **مرة واحدة لكل تبديل** داخل نافذة التحقّق، ويُعفى من مهلة الحجز، لكنه **يُدخل المسار المصدر في تهدئة مضاعفة** — لمنع نمط `A→B→A→B`.

---

### RE-6 — تصنيف القرار ومميّز إعادة التوجيه

**هذا يحلّ X-6 — التناقض المركزي في دراسة المحرك.**

الدراسة تعلن: *«زمن تبديل ≈ 43ms (يحقق <500ms بفارق مرتبة)»*. لكن مع استقصاء 1Hz وتأكيد 3 جولات، الزمن الفعلي:

```
كشف     1–2 s   +   تأكيد   3 s   +   تبديل   0.043 s   ≈   4 s
```

أي **تجاوز العتبة بـ8×**، لا تحقيقها بمرتبة. **الرقم 43ms صحيح — لكنه يقيس الخطوة الأخيرة وحدها، وهي أرخصها وأقلها أهمية للاعب.**

**الحل: ثلاث فئات قرار بميزانيات منفصلة.**

| الفئة | المحفّز | **ميزانية الزمن** | مصدر الإشارة | التحقيق |
|---|---|:---:|---|:---:|
| **Failover** | فقد كامل: 3 PTO متتالية أو انعدام ACK لـ`3×PTO` | **< 500 ms** | مستوى البيانات (فوري) | ✅ **يُحقَّق** |
| **Degradation** | قفزة `owd_var` درجية ≥ 25ms أو `loss` ≥ 5% | **< 2 s** | RE-1 على المسار النشط | ✅ يُحقَّق |
| **Optimization** | فارق درجة ≥ 20% مستقر | **5–15 s** | RE-2 على المرشحين | ✅ مقصود |

> **إعادة الصياغة الصحيحة للعتبة:** *«عتبة 500ms تنطبق على فئة Failover حصراً، وتُحقَّق. فئة Optimization ميزانيتها 5–15 ثانية عمداً — لأن مطاردة تحسّن 10ms بسرعة تُنتج تذبذباً أضرّ من الفارق نفسه.»*

**مميّز إعادة التوجيه عن الازدحام** (يُغذّي فئة Degradation):

| الإشارة | ازدحام | **إعادة توجيه ISP** |
|---|---|---|
| شكل ارتفاع `owd_var` | تدريجي تراكمي | **قفزة درجية لحظية** |
| الارتباط بـ`inflight/cwnd` | مرتفع | **معدوم** |
| الاتجاهية (RE-1) | كلا الاتجاهين غالباً | **اتجاه واحد غالباً** |
| PMTU | ثابت | قد يتغيّر |
| `ecn_ce_count` | يرتفع | ثابت — **⚠️ معطّل حالياً (X-14)** |

المخرَج: `ControlEvent::PathEventDetected { kind, owd_step_us, direction }`.

**الأثر العملي:** إعادة التقييم خلال **~200ms** بدل انتظار دورة تسجيل كاملة — أي أن زمن التعافي من سوء توجيه مزوّد الاتصال ينزل من «دقائق سلوك الـISP» إلى ثوانٍ.

---

### RE-7 — التحقّق بعد التبديل والتراجع التلقائي

**المشكلة (X-17 / INV-20):** ECMP يُجزّئ الحركة على تجزئة الـ5-tuple. **اتصال القياس (RE-2) له منفذ مصدر مختلف ⟹ قد يعبر مساراً فيزيائياً مختلفاً عن التدفّق الحقيقي.** أي أن قياس المرشّح — مهما كان دقيقاً — **لا يضمن** أن التدفّق بعد الهجرة سيسلك المسار المقيس.

هذا قيد بنيوي في **كل** أنظمة القياس خارج النطاق، ولا يمكن إزالته مسبقاً. **يُعالَج بعدياً.**

**الآلية:**

```
1. سجّل  baseline = {owd_var_p50, owd_var_p95, jitter, loss}  للمسار الحالي
2. نفّذ الهجرة                                    (epoch++ ، إعادة بذر المقاييس)
3. VERIFYING: قِس سلبياً لمدة verify_window        (2–5s حسب الفئة)
4. قارن الواقع بـ baseline:
       أفضل أو مساوٍ ضمن الهامش  →  HOLD ثم STEADY   ✅
       أسوأ بأكثر من revert_margin →  REVERTING       ↩︎
5. عند التراجع: المسار المصدر يدخل تهدئة مضاعفة، والحدث يُسجَّل لمعايرة الأوزان
```

**المعاملات:** `revert_margin = 15%` · `verify_window = 2s / 3s / 5s` حسب الفئة · **ميزانية تراجع = 1 لكل تبديل**.

> **هذه الآلية غائبة عن الوثائق الثلاث السابقة، وهي — عملياً — الحماية الوحيدة ضد قرار مبني على قياس غير أمين.** وبدونها، أي فارق بين المسار المقيس والمسار المُحقَّق يظهر لدى اللاعب كتدهور لا يفسّره النظام.

---

### RE-8 — حاكم MTU المقيّد

**المشكلة (X-10 / INV-19):** التجزئة **غير منفَّذة** (`total_fragments`/`fragment_id` دكور)، وFR-1 يرفض `PayloadTooLarge`. ⟹ تخفيض السقف الفعّال أثناء الجلسة يجعل رسائل كان التطبيق يرسلها بنجاح **تفشل فجأة**.

**القواعد الملزِمة:**

| القاعدة | البيان |
|---|---|
| **MTU-1** | السقف الفعّال للجلسة = **الحدّ الأدنى عبر كل المرشحين المؤهَّلين**، يُحسب مرة عند بدء الجلسة |
| **MTU-2** | **يُمنع الخفض أثناء الجلسة.** الاكتشاف بالرفع فقط، ولا يُطبَّق الرفع إلا على جلسة جديدة |
| **MTU-3** | مرشّح لا يستطيع حمل السقف الحالي **يُستبعَد من مجموعة المرشحين** — لا يُخفَّض السقف من أجله |
| **MTU-4** | إطار `MtuProbe` **يجب أن يكون آخر إطار في الداتاغرام** — فكّه يبتلع بقية الحمولة |
| **MTU-5** | حساب السقف من `max_message_payload()` الحقيقية لا من تقدير |

> **الأثر:** يتحوّل M8 من «تحسين» إلى «قيد اختيار مرشّح» — تصنيف أدقّ وأسلم، ويحفظ INV-19 تماماً.

---

### RE-9 — تكامل `total_stale_drops` في التسجيل

**الأساس:** المجدول يطبّق رفضاً استباقياً عندما `now + RTT/2 + queue_delay ≥ deadline`، ويحصي النتيجة في `total_stale_drops` — **معروض أصلاً في `DetailedMetrics`**.

**لماذا هو الأثمن:** كل المقاييس الأخرى **وكيلة** عن تجربة اللاعب. `stale_drop_pct` هو **نسبة رسائل اللعب التي وصلت بلا فائدة** — ليس وكيلاً عن التجربة، بل التجربة نفسها.

```
مسار A:  rtt = 46ms ، stale_drop = 2.1%
مسار B:  rtt = 54ms ، stale_drop = 0.3%
```

نموذج قائم على RTT يختار A دائماً. **B هو الصحيح للاعب.** هذا يحقّق تنفيذياً مبدأ الورقة المفاهيمية §18 («لا تعتمد على Ping وحده») بمقياس موجود ومجاني.

**الاشتقاق:** `stale_drop_pct = Δtotal_stale_drops / Δmessages_enqueued` عبر النافذة. **متاح للمسار النشط فقط** (اتصالات القياس لا تحمل حركة لعب) ⟹ يدخل الدرجة كـ**عقوبة على المسار الحالي**، لا كمحور مقارنة بين المرشحين. وهذا كافٍ: دوره إشعال قرار «اهرب من هذا المسار»، لا ترتيب البدائل.

---

### RE-10 — مخطط القياس (Telemetry)

| الحقل | النوع | المصدر | الاستخدام |
|---|---|---|---|
| `path_id`, `epoch` | u8, u8 | المحرك | INV-13 |
| `rtt_p50_us`, `rtt_p95_us` | u32 | RttStats/مسبار | RE-4 |
| `owd_var_p50_us`, `owd_var_p95_us` | u32 | **RE-1** | RE-4، RE-6 |
| `jitter_us` | u32 | **RE-1 (RFC 3550)** | RE-4 |
| `loss_ppm` | u32 | دفعة المسبار | RE-4 |
| `stale_drop_ppm` | u32 | **RE-9** | RE-4 |
| `reorder_ppm` | u32 | دفعة المسبار | تشخيص |
| `score` | f32 | RE-4 | القرار |
| `state` | enum | RE-5 | تشخيص |
| `switch_count`, `revert_count` | u32 | RE-5/RE-7 | INV-16 |
| `detect_ms`, `confirm_ms`, `switch_ms` | u32 | RE-5 | **INV-16 — الثلاثة منفصلة إلزامياً** |
| `tunnel_advantage_ms` | i32 | مسار مرجعي | حوكمة |

> **`detect/confirm/switch` تُصدَّر منفصلة إلزامياً** — تجميعها في رقم واحد هو بالضبط مصدر X-6.

---

## 8. النهج التنفيذي القابل للتتبّع

### 8.1 المبادئ

| # | المبدأ |
|---|---|
| **P1** | **إصلاح الدرزات التي يعتمد عليها القياس قبل بناء القياس.** محرك على مدخلات فاسدة أسوأ من لا محرك |
| **P2** | **كل مرحلة تنتهي ببوابة قابلة للتنفيذ آلياً.** لا انتقال بلا اجتياز |
| **P3** | **الظل قبل الفعل.** كل آلية قرار تعمل في وضع مراقبة قبل السماح لها بالتنفيذ |
| **P4** | **مفتاح تعطيل واحد يعيد الأساس بالضبط** (INV-15)، ويُختبر سلبياً في كل مرحلة |
| **P5** | **قياس الأساس أولاً.** لا حكم على تحسّن بلا خطّ أساس موثّق على نسخة معتمدة |

### 8.2 المراحل والبوابات

---

#### **G0 — تنظيف الأساس** ⏱ 3–5 أيام · 🔴 حاجز

| البند | الوصف | الكلفة |
|---|---|---|
| **N-1** | بوابة `is_long_header()` قبل فحص أنواع المصافحة | سطر + اختبار — **مخاطرة الانحدار منتفية (V-7)** |
| **X-1/C-2** | `RttStats::reset_for_new_path()` + حارس `Option` بدل `u64::MAX` | ~10 أسطر |
| **N-4** | فصل PTO عن RTO (لا `on_timeout` من مسار PTO) | كلفة **سالبة** — يغلق FU-4 |
| **X-2** | حسم تعارض AAD وتصحيح `GTP-SEC-01` | وثائقي |
| **X-3** | تصحيح وصف الـnonce + توثيق ثابت «مفتاح ⟺ اتصال» | وثائقي |

**بوابة G0:**
- ✅ 5000 رسالة `Unreliable` على loopback ⟹ فقد **< 0.05%** (يفشل قبل الإصلاح)
- ✅ اختبار: هجرة إلى مسار أبطأ ⟹ `min_rtt` يتبع الجديد و`rtt_inflation → ~1.0`
- ✅ اختبار: PTO كاذب مفرد **لا** يخفض `cwnd`؛ الانهيار فقط بعد 2+ PTO متتالية تغطي النافذة
- ✅ `GTP-SEC-01` يطابق `connection.rs:918-922`
- ✅ 91/0 + clippy + fmt

> **لا يجوز البدء بأي عمل قياس قبل اجتياز G0.** N-1 يلوّث كل درجة بضوضاء 1.17%، وX-1 يُفسد الضغط الخلفي بعد أول هجرة. القياس فوقهما هندسة على رمل.

---

#### **G1 — تفعيل طبقة القياس (RE-1)** ⏱ 5–7 أيام

- استهلاك `timestamp_micros` في RX + `base_d` منزلق (نافذة 30s)
- jitter وفق RFC 3550 §6.4.1
- `ControlEvent::OwdSample`
- إضافة `owd_var` و`jitter` إلى `DetailedMetrics`

**بوابة G1:**
- ✅ اختبار وحدة: حقن حزم بتأخير معلوم في `gtp-sim` ⟹ `owd_var` ضمن **±1ms** من المحقون
- ✅ اختبار حافة الالتفاف: طابع زمني عند `2^32 − ε` ⟹ لا قفزة زائفة
- ✅ اختبار الانحراف: 30s عند 50ppm ⟹ خطأ ≤ 1.5ms
- ✅ **صفر تخصيص** في مسار RX (`debug_assert` أو قياس)
- ✅ `jitter` مقاس من حركة لعب حقيقية عند 60Hz **≠ 0** ومستقر

---

#### **G2 — توسعة المحاكاة متعددة المسارات** ⏱ 3–5 أيام

`SimulatedNetwork` اليوم أنبوب واحد (`BinaryHeap` + XorShift64 مزروع). التوسعة:

```rust
pub struct SimulatedFabric {
    links:  Vec<SimulatedLink>,                        // مسار لكل مرشّح
    script: Vec<(MonotonicTime, usize, ProfileDelta)>, // إضرار مبرمج زمنياً
    master_seed: u64,                                  // بذرة كل رابط = splitmix64(master, i)
}

pub struct SimulatedLink {
    fwd: NetworkProfile,     // ← ملف مستقل لكل اتجاه: شرط اختبار RE-1/M6
    rev: NetworkProfile,
    queue: BinaryHeap<SimulatedPacket>,
    rng_state: u64,
}
```

**ثلاث إضافات إلزامية:**
1. **ملف إضرار مستقل لكل اتجاه** — بدونه **يستحيل** اختبار الكشف الاتجاهي (RE-1/M6) أصلاً
2. **جدول إضرار مبرمج زمنياً** — `t=30s: link0 += 40ms +5% loss`
3. **عنق زجاجة مشترك اختياري** — الفجوة التي رصدها التدقيق (سلوك CC تحت تنافس فعلي غير مختبر)

**بوابة G2:** حتمية كاملة — تشغيلان بنفس البذرة ⟹ تسلسل أحداث متطابق بايتاً ببايت.

---

#### **G3 — وضع الظل (قياس بلا قرار)** ⏱ 7–10 أيام

- `gtp-route` نقياً: `PathStats` + `Scorer` (RE-4) + هيكل `DecisionFSM`
- `gtp-route-tokio`: اتصالات قياس (RE-2) + مسبار دفعي + استقصاء 1Hz
- **الحقبة محلية** (RE-3 مبسّطاً — بلا تغيير سلك)
- **يسجّل الدرجات والقرارات الافتراضية · لا يهاجر إطلاقاً**

**بوابة G3:**
- ✅ 24 ساعة ظل على WAN ⟹ سجل درجات كامل، **صفر هجرة**
- ✅ الأداء مطابق للأساس ضمن ضوضاء القياس (INV-15 — اختبار سلبي)
- ✅ `jitter` المقيس بالدفعة ≈ المقيس سلبياً على نفس المسار (تحقّق تقاطعي RE-1 ↔ RE-2)
- ✅ **معايرة الأوزان** من بيانات الـ24 ساعة (§7 RE-4 نقطة انطلاق لا نهاية)

---

#### **G4 — Failover فقط** ⏱ 5–7 أيام

أضيق فئة قرار وأوضحها: **مسار مات** (3 PTO متتالية / انعدام ACK لـ`3×PTO`).

- بلا تأكيد · بلا hold · بلا مقارنة درجات
- تفعيل C-3 (خانات تحدٍّ متعددة أو إلغاء صريح — X-8)
- تفعيل T1 بدلالته المحسومة (X-9)

**بوابة G4:**
- ✅ اختبار محاكاة: إسقاط المسار النشط ⟹ تعافٍ **< 500ms**، `detect/confirm/switch` مصدَّرة منفصلة
- ✅ اختبار WAN: قطع مسار حقيقي ⟹ الجلسة تنجو، التسليم الموثوق يكتمل
- ✅ لا هجرة كاذبة تحت فقد 5% غير كارثي (10 دقائق ⟹ صفر تبديل)

---

#### **G5 — Degradation + التحقّق البعدي** ⏱ 7–10 أيام

- مميّز إعادة التوجيه (RE-6)
- `RevertGuard` (RE-7) — **إلزامي قبل السماح بأي تبديل غير Failover**
- منع التذبذب الكامل (RE-5)

**بوابة G5:**
- ✅ سيناريو محاكاة: `t=30s` تدهور المسار النشط +40ms/+5% ⟹ هجرة خلال **< 2s**
- ✅ سيناريو التراجع: المرشّح يبدو أفضل بالقياس لكنه أسوأ بعد الهجرة ⟹ **تراجع تلقائي خلال نافذة التحقّق**
- ✅ اختبار استقرار: 30 دقيقة بتذبذب RTT ±5ms حول 50 ⟹ **`switch_count = 0`** (اختبار X-7)
- ✅ اختبار الاتجاهية: تدهور اتجاه العودة فقط ⟹ يُشخَّص صحيحاً ولا يُنتج هجرة عمياء للـEntry

---

#### **G6 — Optimization + الحوكمة** ⏱ 7–10 أيام

- فئة التحسين الكاملة بعتباتها البطيئة
- RE-8 (حاكم MTU المقيّد) · RE-9 (`stale_drop` في التسجيل) · المسار المرجعي (`tunnel_advantage_ms`)

**بوابة G6:**
- ✅ `switch_count ≤ 2/دقيقة` تحت كل السيناريوهات
- ✅ `post_switch_delta > 0` في **≥ 80%** من التبديلات (التبديل يحسّن فعلاً)
- ✅ `tunnel_advantage_ms > 0` — أو المحرك يوصي بالمسار المباشر
- ✅ **صفر فشل `PayloadTooLarge`** ناتج عن قرار توجيه (INV-19)

---

## 9. استراتيجية الاختبار

### 9.1 المستويات

| المستوى | النطاق | الحتمية | يرصد |
|---|---|:---:|---|
| **L-U** وحدة | دالة/بنية | كاملة | منطق داخل طبقة |
| **L-I** التحام | طبقتان فأكثر | كاملة | **درزات D1–D2** |
| **L-V** ثابت | ثابت واحد عبر الطبقات | كاملة | **درزات D3–D6 ← الفئة الغائبة اليوم** |
| **L-S** محاكاة | نظام كامل، مسارات متعددة | كاملة (بذرة) | سلوك زمني |
| **L-W** WAN | نشر حقيقي | لا | الواقع |
| **L-N** سلبي | تعطيل الميزة | كاملة | INV-15 |

> **الفجوة المؤسسية: طبقة L-V غير موجودة اليوم إطلاقاً.** وهي الوحيدة القادرة على رصد 9 من 11 عيباً في مصفوفة §3.5. **إنشاؤها أهم مخرَج اختباري في هذه الخطة.**

### 9.2 اختبار لكل ثابت (L-V)

| الثابت | الاختبار | البوابة |
|---|---|:---:|
| INV-1 | `debug_assert` انحراف inflight + اختبار تصريف PTO | ✅ قائم |
| INV-2 | مرآة RTT في cc بعد ACK | ✅ قائم |
| **INV-2b** | **هجرة لمسار أبطأ ⟹ `min_rtt` يتبع، `rtt_inflation → 1.0`** | **G0** |
| INV-3 | حزمة مزوّرة لا تحرق النافذة (✅) + **لا ترفع سقف التضخيم** | G4 |
| **INV-4** | **5000 unreliable ⟹ فقد < 0.05%** | **G0** |
| **INV-5** | **اتصالان، مستهلك واحد متوقّف ⟹ إنتاجية الثاني بلا تأثر** ← يصحّح X-13 | G0+ |
| **INV-6** | **ضغط P1 دائم ⟹ P3/P4 > 0**؛ والحصص مقابل مقام **55** لا 85 (X-11) | G0+ |
| **INV-7** | **PTO كاذب مفرد لا يخفض cwnd** | **G0** |
| INV-8 | مخزن ترتيب بفجوة + زمن متقدم ⟹ تخطٍّ أو مهلة | لاحق |
| INV-9 | خاصية: `(key, nonce)` فريد عبر 10⁶ حزمة | G0 |
| **INV-10** | **تفتيت الترويسة يُفشل المصادقة — لكل حقل** | **G0** |
| **INV-11** | **`RttStats` المرشّح ≠ `RttStats` الرئيسي** | G3 |
| **INV-13** | **عيّنة من حقبة سابقة تُهمَل** | G3 |
| **INV-14** | **تشغيل P=4 مسبارات ⟹ `cwnd` الرئيسي بلا تأثر** | G3 |
| **INV-15** | **تعطيل المحرك ⟹ سلوك مطابق للأساس** | كل بوابة |
| **INV-16** | **`detect/confirm/switch` مصدَّرة منفصلة وتحت السقف لكل فئة** | G4–G6 |
| **INV-17** | `stale_drop` يغيّر ترتيب المسارات في حالة اختبار مصمَّمة | G6 |
| **INV-18** | **grep آلي في CI: كل حقل سلك له مستهلك** | G1 |
| **INV-19** | **صفر `PayloadTooLarge` ناتج عن قرار توجيه** | G6 |
| **INV-20** | **مرشّح يبدو أفضل ويتبيّن أسوأ ⟹ تراجع تلقائي** | G5 |

### 9.3 بروتوكول قياس WAN

موروث من `GTP-rs_TrustedVersion_WAN_Test_AR.md` ومعزَّز:

1. توثيق SHA محلياً وعلى الـVPS · تطابق إجباري · 91/0 على الطرفين قبل أي جولة
2. **خطّ أساس محدَّث بعد G0** — كل قياسات ما قبل G0 مشوّهة بـ1.17% ولا تصلح للمقارنة
3. كل جولة تسجّل: SHA · بذرة · فئة القرار · `detect/confirm/switch` منفصلة · `switch_count` · `revert_count` · `post_switch_delta`
4. **جولة ظل موازية إلزامية** لكل جولة فعل — للمقارنة تحت نفس ظروف الشبكة

---

## 10. مصفوفة التتبّع

| المطلب | المصدر | الثابت | التصميم | الاختبار | البوابة | الحالة |
|---|---|---|---|---|:---:|:---:|
| توجيه بحقول مصادَقة | N-1 | INV-4 | بوابة `is_long_header` | INV-4 | G0 | 🔴 |
| صلاحية `min_rtt` | X-1 | INV-2b | C-2 | INV-2b | G0 | 🔴 |
| فصل PTO/RTO | N-4 | INV-7 | إزالة `on_timeout` | INV-7 | G0 | 🔴 |
| اتساق AAD | X-2 | INV-10 | تصحيح المواصفة | INV-10 | G0 | 🔴 |
| ثابت المفتاح/الاتصال | X-3 | INV-9 | توثيق | INV-9 | G0 | 🔴 |
| قياس OWD/jitter | X-4، X-5 | INV-18 | **RE-1** | INV-18 | G1 | ⚪ |
| محاكاة متعددة المسارات | — | — | **G2** | حتمية | G2 | ⚪ |
| نطاق المقياس = نطاق القرار | X-8، INV-11 | INV-11 | **RE-2** | INV-11 | G3 | ⚪ |
| وسم الحقبة | — | INV-13 | **RE-3** | INV-13 | G3 | ⚪ |
| تسجيل بلا انقطاعات | X-7 | — | **RE-4** | استقرار 30د | G3/G5 | ⚪ |
| مقياس تجربة اللاعب | X-15 | INV-17 | **RE-9** | INV-17 | G6 | ⚪ |
| عدالة المجدول | N-3، X-19 | INV-6 | FR-7 + سقف عجز | INV-6 | G0+ | 🔴 |
| نطاق انفجار التزامن | N-2، X-13 | INV-5 | `try_send` + فكّ القفل | INV-5 | G0+ | 🔴 |
| ميزانية زمن القرار | X-6 | INV-16 | **RE-6** (3 فئات) | INV-16 | G4–G6 | ⚪ |
| أمانة المسار المقاس | X-17 | INV-20 | **RE-7** | INV-20 | G5 | ⚪ |
| استقرار عقد API | X-10 | INV-19 | **RE-8** | INV-19 | G6 | ⚪ |
| إحياء ECN | X-14 | INV-18 | إحياء `gtp-io` أو تمرير ECN | INV-18 | لاحق | ⚪ |
| نطاق تدقيق التوثيق | X-18 | — | توسيع §7 لـ`docs/specs/` | مراجعة | لاحق | ⚪ |

---

## 11. سجل المخاطر

| # | الخطر | الاحتمال | الأثر | التخفيف |
|---|---|:---:|:---:|---|
| R-1 | **قرار توجيه على مدخلات فاسدة** (قبل G0) | مؤكَّد | **حرج** | G0 بوابة حاجزة — لا استثناء |
| R-2 | **حلقة تذبذب مُولَّدة ذاتياً** (N-4 × X-1 — §5.3) | عالٍ | **حرج** | إصلاح الاثنين معاً في G0؛ اختبار استقرار 30د |
| R-3 | **قياس غير أمين بفعل ECMP** (X-17) | عالٍ | عالٍ | **RE-7 إلزامي** قبل أي تبديل غير Failover |
| R-4 | مسبارات تلوّث CC البيانات | متوسط | عالٍ | RE-2 (اتصالات منفصلة) + اختبار INV-14 |
| R-5 | قرار توجيه يكسر عقد API عبر MTU | متوسط | عالٍ | RE-8 (منع الخفض) + اختبار INV-19 |
| R-6 | P اتصالاً إضافياً يستنزف موارد الخادم | متوسط | متوسط | P ≤ 4؛ اتصالات القياس من العميل لا الخادم؛ تقليم |
| R-7 | أوزان RE-4 غير معايرة ⟹ قرارات رديئة | **عالٍ** | متوسط | معايرة إلزامية من بيانات ظل 24 ساعة (G3) |
| R-8 | إعادة ضبط مضاد التضخيم إلى unvalidated ⟹ انهيار إنتاجية | متوسط | عالٍ | حسم دلالة T1 نصّياً في G4 (X-9) |
| R-9 | مشاركة مفتاح مستقبلاً ⟹ إعادة استخدام nonce | منخفض | **كارثي** | توثيق INV-9 صراحةً + فحص CI |
| R-10 | تقارير `docs/` تُعتمد في قرار | متوسط | عالٍ | وسمها «غير صالحة كدليل» حتى إعادة التوليد |

---

## 12. التصحيحات الوثائقية المطلوبة

| # | الموضع | التصحيح |
|---|---|---|
| 1 | `GTP-SEC-01` | AAD = **الترويسة الصريحة كاملة** (يشمل `timestamp_micros` و`payload_len`) |
| 2 | `GTP-SEC-01` + `aead.rs` تعليق | nonce = `IV ⊕ (CID_be[0..4] ‖ PN_be[0..8])` — **4 بايتات فقط**؛ التفرّد من PN وحده |
| 3 | جديد — أي مواصفة أمنية | توثيق **INV-9: «مفتاح واحد ⟺ اتصال واحد»** بوصفه ثابتاً ملزِماً |
| 4 | `GTP-WIRE-01` | `PADDING = 0x00` لا `0x0E`؛ لا وجود لـ`0x0E` |
| 5 | `GTP-CC-01` + README | β الفعلي في `competitive_fps()` = **0.75** لا 0.7 |
| 6 | README + `GTP-PATH-01` | التحقق من المسار **ثنائي** لا «3-Way» |
| 7 | README:93 + `main.rs:25` | hex الـdissect المعطوب → `8000010001 1C … 0009 …` |
| 8 | دراسة المحرك §3 M2 | حجم المسبار **77B** لا ~40B |
| 9 | دراسة المحرك §5.4 | الحصص العادلة بمقام **55** لا 85 |
| 10 | التدقيق §5.2 | النسبة المجمّعة **1.078%** لا 1.10%؛ وحسم تناقض 0.40% (X-12) |
| 11 | التدقيق §4.8 | إعادة صياغة دليل N-2 حول **نطاق الانفجار** لا الـ37.85% |
| 12 | دراسة المحرك §3 M9، §6 | «تهوّر TCP-friendliness» → **«إنصاف/مجاملة TCP»** (الدلالة معكوسة) |
| 13 | دراسة المحرك §0، §5.3 | «صفر تعديل نواة» → **«~100 سطر بكلفة صافية سالبة»** (§6.3) |
| 14 | دراسة المحرك §3 M14 | فصل عتبة 500ms على فئة Failover حصراً (X-6 / RE-6) |
| 15 | التدقيق §7 | توسيع النطاق ليشمل `docs/specs/` العشر (X-18) |

---

## 13. الخلاصة

**ثلاث نتائج تحكم كل ما سبق:**

**الأولى — العيوب في الدرزات لا الطبقات.** تسعة من أحد عشر عيباً لا يرصدها أي اختبار وحدة أو طبقة. هذا يفسّر بدقة كيف تجتاز الشجرة 91/0 اختباراً وclippy نظيفاً بينما تُسقط 1.17% من حركتها صامتةً. **البوابة الخضراء تُثبت سلامة الطبقات، لا سلامة التكامل.** والعلاج ليس مزيداً من اختبارات الوحدة — بل **طبقة اختبار جديدة (L-V) موجّهة للثوابت العابرة**، وهي غير موجودة اليوم.

**الثانية — الخطورة دالّة في السياق.** ثلاثة بنود مصنّفة «منخفضة» أو «اختيارية» تنقلب إلى سدّية لحظة تشغيل المحرك: `min_rtt`، وخانة التحدّي، ودلالة مضاد التضخيم. وأخطرها تفاعلٌ لم ترصده أي وثيقة: **N-4 مع X-1 يشكّلان حلقة تذبذب مُولَّدة ذاتياً لا تمنعها أي آلية hysteresis**، لأن التدهور الذي يقرؤه المحرك حقيقي — وسببه فقط داخلي. لهذا وحده، **G0 بوابة حاجزة غير قابلة للتجاوز**.

**الثالثة — أثمن ما يحتاجه المشروع موجود فيه ومهدور.** `timestamp_micros`: طابع زمني ميكروثانوي في ترويسة كل حزمة، داخل الـAAD، **مؤكَّد بالتحقق المباشر أنه بلا مستهلك واحد**. قراءته وحدها تسدّ الفجوة القياسية التي تفصل المحرك المقترح عن قابليته للعمل: تعطي jitter حقيقياً بمعدل التِك حيث يعجز الاستقصاء 1Hz، وتعطي الفصل الاتجاهي الذي **يستحيل** الحصول عليه من المسبارات مهما بلغت كثافتها.

**والنهج الصحيح ليس «أصلح ثم ابنِ»** — بل تسلسل مشتقّ من بنية الاعتماد نفسها:

```
أصلح ما يفسد القياس   →   فعّل القياس   →   قِس بلا قرار   →
قرّر في أضيق فئة       →   وسّع القرار   →   تحقّق بعد كل قرار
        G0                    G1              G3
        G4                    G5              G6
```

وبهذا يحصل GTP-rs على ذكاء التوجيه الكامل الوارد في الورقة المفاهيمية — **بكلفة صافية سالبة على النواة** (الإصلاحات تُزيل عمليات أكثر مما تضيف)، وبقدرة كاملة على العودة إلى السلوك الأساس بمفتاح واحد.

---

## ملحق أ — فهرس الأدلة المرجعية

| النتيجة | الموقع |
|---|---|
| `timestamp_micros` بلا مستهلك | `gtp-wire/src/header.rs:70,145,218` · `gtp-cli/src/main.rs:108` — **ولا شيء غيرهما** |
| AAD = الترويسة كاملة | `gtp-core/src/connection.rs:311, 918, 922` |
| nonce من CID[0..4] | `gtp-crypto/src/aead.rs` — `for i in 0..4 { nonce[i] ^= cid_bytes[i]; }` |
| `min_rtt` بلا إعادة ضبط | `gtp-recovery/src/rtt.rs:23, 40` |
| `min_rtt` يغذّي الضغط الخلفي | `gtp-cc/src/backpressure.rs:23-24` |
| إطار مجهول = خطأ قاتل | `gtp-wire/src/frame.rs:709` |
| المصافحة بترويسة طويلة | `gtp-runtime-tokio/src/endpoint.rs:113, 171, 414` |
| `gtp-io` متجاوَزة | `endpoint.rs:14, 39` — `tokio::net::UdpSocket` |
| انهيار DRR + تراكم العجز | `gtp-scheduler/src/scheduler.rs` — حلقة `for _ in 0..tiers.len()` |
| PTO → `on_timeout` | `gtp-core/src/connection.rs:722-723` |
| ECN غائب في زمن التشغيل | `crates/gtp-runtime-tokio/src/` — صفر نتيجة |

## ملحق ب — مسرد المعرّفات

| البادئة | المعنى | المصدر |
|---|---|---|
| `FR-n` / `FU-n` / `R-n` | متطلبات ومتابعات وإصلاحات | المواصفة التقنية |
| `N-n` | عيوب التدقيق الشامل | تقرير التدقيق |
| `M-n` | آليات الورقة المفاهيمية | `GTP++.md` |
| `T-n` | لمسات النواة الاختيارية | دراسة المحرك |
| **`INV-n`** | **ثوابت التكامل** | **هذه الوثيقة §3.4** |
| **`X-n`** | **نتائج التدقيق المتقاطع** | **هذه الوثيقة §5** |
| **`RE-n`** | **آليات محرك التوجيه** | **هذه الوثيقة §7** |
| **`V-n`** | **بنود التحقق المباشر** | **هذه الوثيقة §2.3** |
| **`G-n`** | **بوابات التنفيذ** | **هذه الوثيقة §8.2** |
| **`D-n`** | **أنواع الدرزات** | **هذه الوثيقة §3.3** |

---

*نهاية الوثيقة — GTP-rs-ICD-01 v1.0*
