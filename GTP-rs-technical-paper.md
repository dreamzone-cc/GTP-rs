# ورقة تقنية شاملة: الأعمال المتبقية، التحسينات، والاختبارات الإضافية — GTP-rs

**الحالة عند إعداد هذه الورقة:** الكوميت `d3a8a4b` (بعد تنفيذ إصلاحات AEAD/enum/decode-path وإضافة تقرير الضغط).
**المنهجية:** كل بند أدناه تم التحقق منه مقابل الكود المصدري الفعلي، وليس افتراضًا نظريًا. كل بند يُعالَج بشكل مستقل بصيغة موحّدة: **المشكلة / لماذا تهم / الحل المقترح / الكرايتات المتأثرة / الأثر على الأداء والاستقرار / معيار القبول**.

---

# القسم أ: أعمال حرجة غير منجزة (Blocking — يجب إنجازها قبل أي إصدار إنتاجي)

## أ.1 — تبادل مفاتيح فعلي (Key Exchange) لإغلاق فجوة السر الجذري الثابت

**المشكلة:** `ConnectionHot::new` يستدعي `new_with_master_secret` بسر ثابت مكتوب في الكود العلني (`b"gtp_default_session_master_secret_2026"`). HKDF يعمل بشكل صحيح رياضيًا، لكن بما أن هذا السر معروف للجميع (المستودع علني) وبما أن `ConnectionId` يُرسَل بنص واضح في كل حزمة، فإن أي مراقب شبكي يستطيع اشتقاق نفس مفتاح الجلسة لأي اتصال.

**لماذا تهم:** هذا يُبطل عمليًا كل فائدة التشفير الحالي. لا فرق فعليًا بين هذا وبين عدم التشفير إطلاقًا من منظور مهاجم يقرأ الكود المصدري (وهو متاح للجميع بحكم كون المشروع مفتوح المصدر).

**الحل المقترح:**
1. إضافة كرايت تبعية `x25519-dalek` (pure Rust، لا `unsafe`).
2. تصميم رسالتَي Handshake جديدتين على مستوى `gtp-wire`: `ClientHello { public_key: [u8;32] }` و `ServerHello { public_key: [u8;32] }`، تُرسلان قبل أي بيانات مشفَّرة (بنص واضح، هذا طبيعي في بروتوكولات Diffie-Hellman).
3. عند استقبال الطرف الآخر لمفتاحه العام، يُحسَب السر المشترك عبر `x25519_dalek::x25519(my_secret, their_public)`، ثم يُمرَّر مباشرة كـ `shared_secret` إلى `derive_session_keys` الموجودة أصلًا — **لا تغيير مطلوب في `kdf.rs` نفسه**، فقط في مصدر السر.
4. `ConnectionHot::new` القديمة (بدون handshake) تبقى موجودة **فقط** لمسار `secure: false` (وضع الاختبار/المحاكاة الداخلية)، ويُمنع استخدامها في أي مسار شبكي حقيقي عبر `#[cfg(test)]` أو تحذير `#[deprecated]` صريح.

**الكرايتات المتأثرة:** `gtp-crypto` (إضافة وحدة `handshake.rs`)، `gtp-wire` (رسالتا Hello جديدتان)، `gtp-core/connection.rs` (منطق تسلسل الـ handshake قبل `Established`)، `gtp-io`/`gtp-runtime-tokio` (تعديل بسيط لإرسال/استقبال رسالتَي Hello قبل الحلقة الرئيسية).

**الأثر على الأداء/الاستقرار:** تبادل X25519 يحدث **مرة واحدة فقط** عند بدء الاتصال (بضع مئات الميكروثانية)، صفر أثر على زمن الحزم أثناء اللعب. لا يغيّر تنسيق الحزم المشفَّرة نفسها.

**معيار القبول:** اختبار تكامل يُنشئ اتصالَين حقيقيَّين (client/server) عبر `x25519`، يتحقق أن المفتاحين المشتقين متطابقين بين الطرفين ومختلفين عن أي تشغيل سابق (سر عشوائي جديد لكل اتصال)، + اختبار أن حزمة مُشفَّرة باعتراض المهاجم (بدون معرفة السر الخاص) لا يمكن فك تشفيرها.

---

## أ.2 — تفعيل حماية Anti-Amplification فعليًا (معطَّلة حاليًا بشكل افتراضي)

**المشكلة المكتشفة حديثًا:** `AntiAmplificationLimiter` (حد RFC 9000 لمنع استخدام الخادم كمُضخِّم DDoS ضد ضحية منتحَلة العنوان) موجود ومكتوب بشكل صحيح في `gtp-path`، **لكن** في `ConnectionHot::new_with_master_secret` يوجد السطر:
```rust
anti_amp.mark_validated(); // Default validated for established sessions
```
أي أن **كل اتصال جديد يُعتبَر "مُتحقَّق منه" فورًا منذ اللحظة الأولى**، ما يُبطل الحماية بالكامل — أي عنوان يستطيع بدء اتصال والحصول على استجابات غير محدودة الحجم منذ الحزمة الأولى، وهذا بالضبط سيناريو هجوم الانعكاس/التضخيم (reflection/amplification attack) الذي صُمم هذا المكوّن أصلًا لمنعه.

**الحل المقترح:** إزالة `mark_validated()` من مسار الإنشاء الافتراضي. يجب أن يبقى الاتصال في حالة "غير مُتحقَّق" حتى يكتمل مسار تحقق فعلي (إما عبر آلية `PathValidator` الموجودة أصلًا، أو عبر إثبات ملكية العنوان ضمن رسالة الـ handshake من البند أ.1 — استلام رد صحيح من العنوان المُدَّعى هو الدليل الوحيد المقبول).

**الكرايتات المتأثرة:** `gtp-core/state.rs` فقط (حذف سطر واحد + ربط `mark_validated()` بنقطة اكتمال الـ handshake الفعلية بدل استدعائه فورًا).

**الأثر على الأداء:** صفر — هذا تغيير منطقي بحت، الحد (3×) لا يُطبَّق أصلًا إلا على الاتصالات غير المُتحقَّقة، وبعد نجاح الـ handshake (بضع مللي ثانية) تُرفَع القيود تلقائيًا كما هو مصمَّم.

**معيار القبول:** اختبار يبدأ اتصالًا جديدًا ويحاول إرسال بيانات من الخادم تتجاوز 3× حجم ما استلمه من العميل **قبل** اكتمال الـ handshake — يجب أن يُرفَض. بعد اكتمال الـ handshake، يجب أن يُسمَح بأي حجم.

---

# القسم ب: تحسينات أمنية إضافية (لم تُطرَح سابقًا، اكتُشفت أثناء المراجعة)

## ب.1 — عدم وجود تدوير مفاتيح (Key Rotation) لجلسات طويلة الأمد

**المشكلة:** مفتاح الجلسة المشتق عبر HKDF ثابت طوال عمر الاتصال بأكمله. جلسات الألعاب الطويلة (ساعات) تُراكم عددًا هائلًا من الحزم على نفس المفتاح، ما يزيد نظريًا فرصة تصادم nonce (رغم أن الاشتقاق من `packet_number` يقلل هذا، لكنه ليس مستحيلًا على مدى مليارات الحزم).

**الحل المقترح:** إضافة آلية اختيارية لإعادة اشتقاق مفتاح جديد (`key_phase` bit في الرأس، مشابه لآلية QUIC) بعد عدد معيّن من الحزم (مثلًا كل 2^32 حزمة) أو زمن معيّن (كل ساعة)، عبر HKDF إضافي يشتق `key_N+1` من `key_N`.

**الكرايتات المتأثرة:** `gtp-crypto/kdf.rs` (دالة `ratchet_key`)، `gtp-wire` (بت إضافي في الرأس)، `gtp-core` (منطق تبديل المفتاح عند استلام تغيير الـ key phase من الطرف الآخر).

**الأثر:** صفر على المسار الساخن العادي؛ عملية التدوير نفسها نادرة الحدوث (كل ساعات) فتكلفتها مهملة.

**الأولوية:** متوسطة (ليست حرجة لجلسات الألعاب القصيرة/المتوسطة النموذجية، لكنها مطلوبة لأي جلسة تتجاوز ساعات).

## ب.2 — عدم وجود حماية 0-RTT/Replay على مستوى الـ Handshake نفسه

**ملاحظة:** `ReplayWindow` الموجود يحمي البيانات المشفَّرة بعد اكتمال الاتصال، لكن رسالتَي Hello (البند أ.1) بنص واضح ولا تحتوي حماية من إعادة الإرسال (replay) على مستوى الـ handshake نفسه — قد يُعيد مهاجم بث رسالة `ClientHello` قديمة. بما أن X25519 يُنتج سرًا مختلفًا لكل زوج مفاتيح عشوائي جديد، هذا ليس خطيرًا مباشرة (السر يبقى فريدًا)، لكن يُستحسَن إضافة nonce/timestamp قصير الصلاحية في رسالة Hello لمنع هجمات إغراق (flooding) بإعادة بث Hello قديمة لإنهاك الخادم بحسابات X25519 غير ضرورية.

**الحل المقترح:** إضافة stateless cookie/token (البنية `StateLessTokenManager` موجودة أصلًا في `gtp-path` لغرض مشابه — **إعادة استخدامها** بدل إنشاء آلية جديدة) يُرسَل في رد أولي قبل قبول أي حساب X25519 مكلف، على غرار آلية QUIC Retry.

**الكرايتات المتأثرة:** `gtp-path` (استخدام `StatelessTokenManager` الموجود فعلًا)، `gtp-core` (دمجه في تسلسل الـ handshake الجديد).

**الأولوية:** منخفضة-متوسطة (حماية دفاع في العمق، ليست حرجة كالبندين أ.1/أ.2).

## ب.3 — مقارنة الوقت الثابت (Constant-Time) عند التحقق من الـ Token/Nonce

**المشكلة:** عند تنفيذ ب.2، يجب التأكد أن أي مقارنة لـ token/cookie تستخدم دالة مقارنة ثابتة الزمن (مثل `subtle::ConstantTimeEq`)، وليس `==` القياسي على المصفوفات، لتجنب تكرار نفس ثغرة التوقيت التي أُصلحت سابقًا في `aead.rs`.

**الحل المقترح:** إضافة تبعية `subtle` (خفيفة جدًا، صفر تبعيات إضافية تقريبًا) واستخدامها في أي مقارنة أمنية جديدة تُضاف ضمن ب.2.

**الأولوية:** منخفضة، لكنها شرط مصاحب إلزامي لتنفيذ ب.2 بشكل صحيح.

---

# القسم ج: تحسينات في بنية الاختبار (تتجاوز ما نُفِّذ حاليًا)

## ج.1 — استبدال حلقة الـ Fuzzing اليدوية بـ `cargo-fuzz` حقيقي (Coverage-Guided)

**المشكلة:** `test_fuzz_mutated_buffers_resilience` الحالي يستخدم مولّد أرقام شبه عشوائي بسيط (LCG) بحلقة ثابتة 1000 تكرار — هذا اختبار تراجعي (regression) مفيد لكنه **ليس fuzzing حقيقيًا**: لا يستكشف مساحة المدخلات بذكاء (لا يتتبع تغطية الكود لتوليد مدخلات جديدة تصل لمسارات غير مُستكشَفة)، وحجم المخازن محدود بـ 64 بايت فقط (`len % 64`)، بينما الحزم الحقيقية أكبر بكثير.

**الحل المقترح:**
```bash
cargo install cargo-fuzz
cargo fuzz init
```
إنشاء هدفين على الأقل:
- `fuzz_targets/decode_frame.rs` → يغذّي `Frame::decode` مباشرة ببايتات عشوائية بأحجام تصل حتى MTU الكامل (1200+ بايت).
- `fuzz_targets/decode_header.rs` → نفس الشيء لـ `PacketHeader::decode`.

يُشغَّل كمهمة CI منفصلة (ليست جزءًا من `cargo test` العادي، بل خطوة إضافية بحد زمني، مثلًا 10 دقائق لكل push على الفرع الرئيسي، أو تشغيل ليلي مجدوَل).

**الأثر على الأداء:** صفر — fuzzing كود منفصل تمامًا في مجلد `fuzz/`، لا يُصرَّف أبدًا ضمن `cargo build --release` العادي ولا يدخل ضمن الـ binary النهائي.

**معيار القبول:** تشغيل fuzzing لمدة ساعة كاملة بدون أي panic أو تسرّب ذاكرة (يُكتشَف عبر AddressSanitizer المدمج تلقائيًا مع cargo-fuzz).

## ج.2 — إضافة `cargo-audit` إلى CI (موصى به سابقًا، لم يُنفَّذ بعد)

**المشكلة:** التبعيات الجديدة (`chacha20poly1305`, `hkdf`, `sha2`, وقريبًا `x25519-dalek`) تحتاج فحصًا دوريًا آليًا لثغرات معروفة (CVEs) عبر قاعدة بيانات RustSec.

**الحل المقترح:** إضافة خطوة في `ci.yml`:
```yaml
- name: Security Audit
  run: cargo install cargo-audit && cargo audit
```

**الأثر:** صفر على الإنتاج، خطوة CI فقط.

## ج.3 — اختبارات تكامل لتبادل المفاتيح (X25519) تحت هجمات محاكاة

بعد تنفيذ أ.1، يجب إضافة اختبارات محددة:
- **اختبار Man-in-the-Middle سلبي:** مهاجم يعترض حزم Hello فقط (بدون تعديل) ويحاول فك تشفير البيانات اللاحقة بدون معرفة أي مفتاح خاص — يجب أن يفشل تمامًا (فحص إحصائي: تحقق أن البيانات المفكوكة عشوائية بالكامل ولا تحتوي أي نمط من النص الأصلي).
- **اختبار حقن `ServerHello` مزوَّر من طرف ثالث:** يجب أن يُنتج مفتاحًا مختلفًا عن المفتاح الحقيقي، ما يجعل أي حزمة لاحقة من الخادم الحقيقي تفشل بالمصادقة (`open()` يُعيد خطأ) — وهذا **دليل غير مباشر جيد**، لكنه يُظهر حاجة مستقبلية لمصادقة الخادم (توقيع الـ `ServerHello`، انظر ج.5) وليس فقط تبادل مفاتيح مجهول (unauthenticated DH) عرضة لهجوم MITM نشط.

## ج.4 — اختبارات تحت حمل متزامن حقيقي (Concurrency Stress)

**المشكلة:** كل اختبارات الأداء الحالية (`StressSuite`) تعمل على اتصال واحد (Single connection) تسلسليًا. لا يوجد اختبار لآلاف الاتصالات المتزامنة على نفس الخادم — وهذا السيناريو الحقيقي لأي خادم ألعاب متعدد اللاعبين.

**الحل المقترح:** إضافة سيناريو جديد في `gtp-cli StressSuite` (وضع `concurrent`): فتح 500-5000 اتصال `GtpEndpoint` متزامن (عبر Tokio tasks)، كل منها يرسل حمل خفيف، وقياس: استهلاك الذاكرة الكلي (يجب أن يتوسّع خطيًا مع عدد الاتصالات، لا أُسّيًا)، زمن استجابة الخادم تحت الحمل الكامل، وعدم وجود أي تنافس أقفال (lock contention) بما أن `ConnectionHot` مصمَّم كحالة معزولة لكل اتصال.

**الكرايتات المتأثرة:** `gtp-cli` (سيناريو جديد)، `gtp-runtime-tokio` (التأكد أن حلقة استقبال UDP الواحدة توزّع الحزم بكفاءة على آلاف الاتصالات دون عنق زجاجة).

**الأثر المتوقَّع:** هذا اختبار كشف (لا إصلاح مسبق)؛ إن كشف عنق زجاجة (مثلًا قفل مشترك على جدول الاتصالات)، يُعالَج حينها ببنية مناسبة (مثل `DashMap` بدل `Mutex<HashMap>`).

## ج.5 — اختبار NAT Rebinding الفعلي عبر شبكة حقيقية (وليس فقط وحدويًا)

**ملاحظة:** `PathValidator` مُختبَر وحدويًا بشكل جيد (رأيت الاختبار)، لكن لم يُختبَر ضمن سيناريو شبكة حقيقي (`StressSuite`) حيث يتغيّر منفذ NAT فعليًا أثناء اتصال جارٍ (محاكاة عبر إعادة ربط socket العميل لمنفذ محلي جديد منتصف الاختبار). يُضاف كسيناريو جديد في `StressSuite` (وضع `nat-rebind`).

## ج.6 — اختبار استنزاف الذاكرة عند اتصالات نصف-مفتوحة (Half-Open Connections)

**المشكلة:** بعد تنفيذ أ.2 (رفع الحماية من الاتصالات غير المُتحقَّقة)، يجب اختبار: ماذا يحدث لو أرسل مهاجم آلاف رسائل `ClientHello` (بدون إكمال أي منها) بمعدل مرتفع؟ هل تتراكم حالة `ConnectionHot` جزئية في الذاكرة بلا حدود (استنزاف ذاكرة/DoS)؟

**الحل المقترح:** فرض حد أقصى على عدد الاتصالات "قيد التحقق" (pending/unvalidated) لكل خادم، مع انتهاء صلاحية زمني قصير (مثلًا 3 ثوانٍ) لأي handshake غير مكتمل، وإسقاطه تلقائيًا. يُختبَر عبر محاكاة إغراق (flood simulation) في `gtp-sim`.

**الكرايتات المتأثرة:** `gtp-core` (منطق انتهاء الصلاحية)، `gtp-sim` (سيناريو الاختبار).

---

# القسم د: تصحيح دقة التقارير والوثائق (لا يمس الكود، لكنه ضروري للمصداقية)

## د.1 — فصل نتائج المحاكاة عن نتائج الشبكة الفيزيائية في التقارير

**المشكلة:** التقرير الحالي (`GTP_COMPREHENSIVE_STRESS_AND_STABILITY_TEST_REPORT.md`) يعرض نتائج `SimulationRunner` (محاكاة داخل العملية) ونتائج الشبكة الفيزيائية الحقيقية (192.168.1.10/.20) ضمن نفس الأقسام دون تمييز واضح.

**الحل المقترح:** أي تقرير مستقبلي يجب أن يحمل عمود/وسم صريح لكل نتيجة: `[SIMULATED]` أو `[PHYSICAL-NETWORK]`، مع قسمين منفصلين تمامًا بدل الدمج.

## د.2 — توضيح طبيعة رقم "Max Throughput" في أي تقرير مستقبلي

**الحل المقترح:** استبدال التسمية "Max Throughput Capacity" بتسمية دقيقة مثل "Local Encode+Enqueue Rate (not end-to-end confirmed delivery)"، مع إضافة قياس منفصل حقيقي لـ end-to-end throughput (حزم مؤكَّد استلامها فعليًا عبر الشبكة الفيزيائية بمرور الوقت، مثل مؤشر Section 13's "Max Sustained Network Throughput: 1.096 MB/s" الذي هو أدق وأصدق من رقم الذروة).

## د.3 — استبدال لغة "الشهادة الرسمية" الذاتية بمصطلحات دقيقة

**الحل المقترح:** استبدال عبارات مثل "PRODUCTION READY (ENTERPRISE GRADE)... officially certified" بصياغة أكثر دقة مثل: "اجتاز مجموعة الاختبارات الداخلية التالية بتاريخ كذا؛ لا يُغني عن مراجعة أمنية مستقلة (third-party security audit) قبل الاستخدام في بيئة إنتاجية تُعالج بيانات حساسة." — خصوصًا أن أ.1 (تبادل المفاتيح) لم يكتمل بعد وقت هذا التقرير.

## د.4 — ترقية رقم الإصدار (SemVer) لتعكس التغيير غير المتوافق للخلف

**المشكلة:** استبدال خوارزمية التشفير بالكامل (XOR → ChaCha20-Poly1305) هو تغيير غير متوافق للخلف في تنسيق الحزم المشفَّرة (أي عميل قديم لن يستطيع التواصل مع خادم جديد أو العكس)، لكن `workspace.package.version` لا يزال `0.1.0`.

**الحل المقترح:** ترقية إلى `0.2.0` فورًا مع `CHANGELOG.md` يوثّق بوضوح: "BREAKING: طبقة التشفير أُعيد بناؤها بالكامل؛ الإصدار 0.1.0 غير متوافق مع 0.2.0 على مستوى wire format".

---

# القسم هـ: تحسينات أداء/كفاءة إضافية (اختيارية، بعد إغلاق القسمين أ وب)

## هـ.1 — GSO/GRO Batching (مذكور في تقرير الاختبار كتوصية مستقبلية — تفصيل التنفيذ)

**الحل المقترح:** استخدام `UDP_SEGMENT`/`UDP_GRO` عبر `socket2` (موجود أصلًا كتبعية) في `gtp-io` لتجميع عدة حزم خرج في نداء نظام (syscall) واحد عند إرسال دفعات كبيرة (مثل بث حالة 100 كيان كل tick). هذا يقلل overhead النظام تحت حمل مرتفع (خوادم بها آلاف اللاعبين المتزامنين) دون تغيير أي منطق بروتوكول.

**الكرايتات المتأثرة:** `gtp-io` فقط (طبقة النقل الخام)، معزولة تمامًا عن منطق CUBIC/الجدولة/التشفير.

## هـ.2 — منطقة "TCP-Friendly" في CUBIC (من الخطة السابقة، لم تُنفَّذ بعد)

كما ورد في القسم 4.3 من الخطة السابقة — لا يزال معلَّقًا، أولوية منخفضة (تحسين عدالة التعايش مع تدفقات أخرى، ليس عيبًا حرجًا).

## هـ.3 — استبدال `Vec<u8>` في `HandshakeSecret` بمصفوفة ثابتة الحجم مع Zeroization

**ملاحظة دقيقة:** `HandshakeSecret { secret: Vec<u8> }` يخصّص على الـ heap ولا يمسح الذاكرة عند إسقاطه (drop) — الأسرار الحساسة (السر المشترك من X25519) يجب أن تُمسَح من الذاكرة فور الانتهاء منها لمنع تسرّبها عبر memory dumps أو ثغرات قراءة ذاكرة لاحقة.

**الحل المقترح:** استخدام crate `zeroize` (خفيف جدًا، شائع في مكتبات التشفير بـ Rust) مع `#[derive(Zeroize, ZeroizeOnDrop)]` على `HandshakeSecret` والأسرار المؤقتة في مسار X25519 (البند أ.1).

**الأثر:** مسح الذاكرة يحدث فقط عند انتهاء الاتصال (drop)، تكلفته مهملة تمامًا (نسخ بضع عشرات البايتات).

---

# جدول التتبّع الشامل (لإدارة التنفيذ)

| # | البند | القسم | الأولوية | يعتمد على |
|---|---|---|---|---|
| أ.1 | تبادل مفاتيح X25519 حقيقي | أمان حرج | 🔴 حرجة | لا شيء (يبدأ فورًا) |
| أ.2 | إلغاء `mark_validated()` الافتراضي | أمان حرج | 🔴 حرجة | أ.1 (لربط التحقق باكتمال handshake) |
| ب.1 | تدوير المفاتيح (Key Rotation) | أمان إضافي | 🟡 متوسطة | أ.1 |
| ب.2 | حماية replay على مستوى Handshake | أمان إضافي | 🟢 منخفضة-متوسطة | أ.1 |
| ب.3 | مقارنة ثابتة الزمن للـ Token | أمان إضافي | 🟢 منخفضة (شرط مصاحب لـ ب.2) | ب.2 |
| ج.1 | `cargo-fuzz` حقيقي | اختبار | 🟡 عالية | لا شيء |
| ج.2 | `cargo-audit` في CI | اختبار | 🟡 عالية | لا شيء |
| ج.3 | اختبارات تكامل X25519 | اختبار | 🔴 حرجة | أ.1 |
| ج.4 | اختبار تزامن آلاف الاتصالات | اختبار | 🟡 متوسطة | لا شيء |
| ج.5 | اختبار NAT rebinding على شبكة حقيقية | اختبار | 🟢 منخفضة | لا شيء |
| ج.6 | اختبار استنزاف نصف-الاتصالات | اختبار | 🟡 متوسطة | أ.2 |
| د.1-د.4 | تصحيح دقة التقارير والإصدار | وثائق | 🟡 متوسطة | مستقلة |
| هـ.1 | GSO/GRO batching | أداء | 🟢 منخفضة | لا شيء |
| هـ.2 | CUBIC TCP-friendly | أداء | 🟢 منخفضة | لا شيء |
| هـ.3 | Zeroization للأسرار | أداء/أمان خفيف | 🟡 متوسطة | أ.1 |

**مسار التنفيذ الموصى به:** (أ.1 + أ.2) بالتوازي مع (ج.1 + ج.2، مستقلان تمامًا) → ج.3 (يعتمد على أ.1) → ب.1/ب.2/ب.3/هـ.3 (كلها تعتمد على وجود handshake حقيقي من أ.1) → ج.4/ج.5/ج.6 (اختبارات كشف مستقلة، يمكن تشغيلها في أي وقت) → د.1-د.4 (مستمرة بالتوازي مع كل ما سبق) → هـ.1/هـ.2 (تحسينات اختيارية أخيرة).

الإغلاق الكامل للبندين أ.1 وأ.2 هو الشرط الوحيد الفاصل بين الوضع الحالي ("تشفير رياضيًا صحيح لكن بلا سرية فعلية") ووضع يمكن فيه فعلًا الحديث عن جاهزية أمنية للإنتاج.
