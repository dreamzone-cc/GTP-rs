# GTP-rs — Comprehensive Audit Findings & Master Remediation Plan
# الورقة التقنية الشاملة: نتائج الفحص المعمّق وخطة الإصلاح المتكاملة لبروتوكول GTP-rs

> **تاريخ الاعتماد والتوثيق:** 4 سبتمبر 2026  
> **نطاق المشروع:** 13 حزمة Rust داخل المستودع (`/home/ggonlinux/GTP`)  
> **مرجعية الفحص:** التحقق المباشر من شفرة المصدر وتنفيذ الاختبارات الحية لكافة الطبقات (دون الاعتماد على الوثائق أو التعليقات القديمة)  
> **بيئة العمل والتطوير:** التطوير والمزامنة محلياً وعبر **Radicle** فقط (يُستثنى النشر على GitHub في هذه المرحلة حتى اكتمال كافة الفحوصات والإصلاحات والاعتماد النهائي)

---

## 1. الأهداف ومعايير النجاح القابلة للقياس (Success Criteria)

1. **تغطية الفحص:** التحقق الفعلي بنسبة **100%** من جميع الوظائف والميزات المدرجة في نطاق الفحص عبر اختبارات برمجية قابلة لإعادة الإنتاج.
2. **إعادة مراجعة الفحص الأول:** تدقيق بنسبة **100%** لجميع نقاط الضعف والمشكلات المرصودة في وثائق الفحص والمراجعات السابقة وتوثيق حالتها بدقة.
3. **سلامة الاختبارات المستقلة (Unit Level):** نجاح بنسبة **100%** للاختبارات المستقلة لكل طبقة ومكون دون أي هلع أو أخطاء غير مبررة.
4. **سلامة اختبارات التكامل (Integration Level):** نجاح بنسبة **100%** لاختبارات التكامل والترابط بين الطبقات لضمان عمل البروتوكول كنظام موحد متكامل.
5. **انعدام الأخطاء الحرجة:** إغلاق جميع الأخطاء الحرجة والعالية الخطورة دون استثناء.
6. **منع الانحدار (No Regressions):** كل إصلاح يُثبت باختبار يبرهن حل المشكلة، مع بقاء كافة الاختبارات الحالية خضراء تماماً.
7. **إدارة النسخ والمستودع:** العمل محلياً عبر Radicle فقط وعدم النشر على GitHub حالياً.

---

## 2. سجل نتائج إعادة التحقق من الفحص الأول والثغرات المكتشفة

يوضح الجدول أدناه الحالة الدقيقة لكل نقطة ضعف تم تدقيقها برمجياً ومخبرياً:

| المعرّف | المشكلة والوصف الفني | الخطورة | الحالة الحالية | الاختبار والتحقق الفعلي |
| :--- | :--- | :---: | :---: | :--- |
| **SEC-1** | إعادة استخدام المفتاح و الـ Nonce لكلا الاتجاهين مما يكسر سرية تشفير ChaCha20-Poly1305 | حرجة | ✅ مغلق | مفاتيح اتجاهية مشتقة بعلامات `c2s` و `s2c` (`directional_keys_distinct_per_role`) |
| **SEC-2** | إهمال أعلى 32 بت من رقم الحزمة عند توليد Nonce مما يسبب تصادماً بعد $2^{32}$ حزمة | عالية | ✅ مغلق | خلط كامل الـ 64 بت في الـ Nonce (`nonce_uses_full_packet_number`) |
| **SEC-3** | تحديث نافذة الإعادة (Replay Window) قبل فك التشفير AEAD مما يمكن المهاجم من حرق النافذة بحزمة مزورة | حرجة | ✅ مغلق | فصل `check` عن `commit` والالتزام بعد نجاح AEAD فقط (`spoofed_high_pn_does_not_burn_replay_window`) |
| **SEC-4** | كوكي المصافحة عديم الحالة قابل للتزوير لكونه XOR وتوقيته غير مصادق | حرجة | ✅ مغلق | كوكي HMAC-SHA256 يرفض التواقيت المستقبلية ويمنع استخراج السر (`test_cookie_verify_rejects_wrong_addr_and_expiry`) |
| **SEC-7 / 10 / 12** | عدم تصفير المفاتيح في الذاكرة وغياب فحص نقاط X25519 الضعيفة وطول التشفير غير الآمن | متوسطة/عالية | ✅ مغلق | تفعيل `zeroize` واشتقاق `was_contributory` وفحص الطول بـ `checked_add` |
| **Core-C1 / PATH-5** | تغليف إطارات التحكم داخل إطار Data مما أدى لتعطيل الميزات عملياً (Close, Ping, PathChallenge) | حرجة | ✅ مغلق | طابور تحكم مخصص وإرسال إطارات التحكم كإطارات سلكية حقيقية (`control_frames_reach_peer_as_frames`) |
| **REC-10** | عدم التحقق من نطاقات إطارات ACK مما يسمح بتخصيص ذاكرة يصل إلى 34GB | حرجة | ✅ مغلق | سقف 16384 ورفض إقرارات أكبر من الحزم المرسلة (`ack_ranges_multi_gap_roundtrip_exact`) |
| **FR-1** | تعليق طوابير الإرسال بسبب حمولات تفوق MTU دون تجزئة | عالية | ✅ مغلق | رفض الحمولات الزائدة قبل استهلاك المعرفات أو حرق أرقام التسلسل (`oversized_payload_is_rejected_and_never_stalls_a_tier`) |
| **FR-2** | استهلاك غير محدود لذاكرة مجموعات الترتيب في مسار الاستقبال | عالية | ✅ مغلق | تحديد سقف المجموعات بـ 256 مع سياسة إخلاء FIFO (`ordered_group_map_stays_bounded_under_many_wire_group_ids`) |
| **FR-3** | تعدد مصادر احتساب الحزم قيد النقل (In-flight bytes) واختلافها بين كاشف الفقد و CUBIC | عالية | ✅ مغلق | توحيد المصدر ليصبح كاشف الفقد هو المرجع الأوحد وإزالة المرآة الموازية (`inflight_is_a_single_source_and_pto_drain_sheds_it`) |
| **FR-4** | تباين تقدير RTT وتكرار تخزين المسار النشط في عدة بنيات | متوسطة | ✅ مغلق | مزامنة RTT مع المتحكم وتوحيد ملكية المسار النشط في `hot.active_path` |
| **N-1** | إسقاط عشوائي صامت بنسبة **1.172%** لجميع داتاغرامات البيانات المشفرة نتيجة فحص بايت التعمية كإطار مصافحة | **حرجة** | 🔧 جاهز بالفرع | مفحوص ومثبت في فرع `fix/New-8` بحصر الفحص على بت الترويسة الطويلة (`short_header_ciphertext_colliding_with_handshake_types_is_still_routed`) |
| **N-2** | حجب رأس الطابور (HoL) في حلقة الاستقبال RX عند توقف مستهلك القناة مما يجمد كافة الاتصالات | **عالية** | 🔧 جاهز بالفرع | مفحوص ومثبت في فرع `fix/New-8` عبر `try_send` وعزل المشترك البطيء (`slow_consumer_test.rs`) |
| **X-1** | عدم تصفير RTT عند هجرة الاتصال إلى مسار جديد | متوسطة | 🔧 جاهز بالفرع | إعادة ضبط مقاييس RTT عند نجاح التحدي واعتماد المسار |
| **A-5** | احتساب بايتات مضاد التضخيم قبل مصادقة الحزم المشفرة | متوسطة | 🔧 جاهز بالفرع | احتساب الرصيد بعد نجاح AEAD فقط |
| **New-8** | رصيد مكافحة التضخيم 3x عام للاتصال وليس مخصصاً لكل مسار (مخالفة RFC 9000 §9.3) | عالية | 🔧 جاهز بالفرع | فصل رصيد الاستطلاع وحصره في فتحة مخصصة للمسار الجاري التحقق منه |
| **New-12** | استغلال `PathChallenge` كعاكس هجمات لتوليد ردود لعناوين عشوائية أو بأعداد ضخمة | عالية | 🔧 في شجرة العمل | قصر الرد على المسار النشط وسقف رد واحد لكل داتاغرام و 2 في طابور التحكم |
| **N-3** | انهيار جدولة DRR إلى أولوية صارمة وتجويع P3 و P4 تماماً تحت ضغط P1 المستمر | **عالية** | 🔴 مفتوح | حلقة `pop_next` تبدأ دائماً من P1 وتخرج فوراً عند توفر رصيد دون مؤشر دورة دائم |
| **N-4** | انهيار نافذة الاحتقان `cwnd` إلى `2*SMSS` عند كل مسبار PTO (مخالفة صريحة لمعيار RFC 9002 §7.5) | **عالية** | 🔴 مفتوح | استدعاء `cc.on_timeout` عند كل إطلاق لمسبار PTO كأنه RTO |
| **FR-8** | وسم جميع الحزم المسترجعة من مخزن الترتيب برقم تسلسل الحزمة الواردة وليس برقمها الحقيقي | متوسطة | 🔴 مفتوح | `OrderedGroupReceiver::on_incoming` تعيد الحمولات دون أرقام تسلسلها المستقلة |
| **N-5** | تسرب القيمة الحارسة `min_rtt = u64::MAX` إلى واجهة المقاييس وحسابات الضغط الخلفي | متوسطة | 🔴 مفتوح | غياب فحص العينة الأولى مما يعطل حساب تضخم RTT ويطبع أرقاماً خيالية في CLI |
| **N-7** | إمكانية إغراق طوابير المجدول ومخزن الترتيب بعناصر فارغة (0 بايت) لتجاوز سقف البايتات | متوسطة | 🔴 مفتوح | التحقق يقارن فقط البايتات دون سقف عددي لعدد الرسائل |
| **WIR-2** | تشوه ترميز إطار ACK بكتابة `range_count` الأصلي بدلاً من القيمة المحدودة بسقف 32 | متوسطة | 🔴 مفتوح | يولد إطاراً يرفضه مفكك الحزم نفسه |
| **WIR-4** | شطر محارف UTF-8 متعددة البايتات عند بتر سبب إغلاق الاتصال عند 255 بايت | متوسطة | 🔴 مفتوح | استخدام شطر بايتات مجرد دون فحص `floor_char_boundary` |
| **SEC-14** | اشتقاق Debug الافتراضي يطبع المفاتيح المشفرة الخام في سجلات النظام والهلع | منخفضة/متوسطة | 🔴 مفتوح | يجب حجب المفاتيح عبر `[REDACTED]` |
| **FR-5** | مخزن الترتيب بلا آلية مهلة للفجوات، والاتصال المغلق Closed يستمر في فك وتوزيع الحزم | متوسطة | 🔴 مفتوح | غياب بوابة حالة في بداية `handle_incoming_datagram` |
| **N-6** | أخطاء التوثيق وأمر dissect المعطوب ونصوص الاختبارات الثابتة | توثيق وجودة | 🔴 مفتوح | طول الترويسة الطويلة 24 بايت في المثال بينما المشترط ≥ 28 بايت |

---

## 3. منهجية التحقق المزدوج (المستقل والتكاملي)

### 3.1 مستوى الاختبار المستقل (Unit Isolation Testing)
- **gtp-wire:** التحقق من ترميز وفك كافة أنواع الإطارات الـ 14، سلامة اقتطاع UTF-8 في إطار Close، انضباط كتابة `range_count` المقلّم في إطار ACK، واختبارات المدخلات العشوائية المشوهة (Fuzz Mutation).
- **gtp-crypto:** التحقق من عزل اتجاهات المفاتيح، استقلالية الـ Nonce عبر كامل مجال 64 بت، تصفير الذاكرة، ورفض المفاتيح العامة ذات الرتبة المنخفضة.
- **gtp-recovery:** اختبارات حساب RTT، دقة كاشف الفقد، ومعالجة فترات ACK المتعددة.
- **gtp-cc:** فحص خوارزمية CUBIC ومطابقتها لمعادلات RFC 8312 لمنحنى W_cubic، منطقة TCP-Friendly، وعدم خفض النافذة عند إقرارات فارغة.
- **gtp-scheduler:** فحص طوابير الأولوية P0..P4، تجريد واستبدال الحالات القديمة (`StateTable`)، وسلوك wrap-around لأرقام التسلسل.
- **gtp-path:** دقة محدد التضخيم 3x، صلاحية وتوقيت كوكي HMAC.

### 3.2 مستوى اختبار التكامل بين الطبقات (Cross-Layer Integration Testing)
- **مسار الاسترداد الكامل (Recovery ↔ Scheduler ↔ Wire ↔ Core):**
  إرسال تدفق حزم موثوقة مرتبة (`ReliableOrdered`) مع حقن فقد متعمد في الحزمة 0، ثم استقبال الحزم 1..5 واحتجازها بمخزن الترتيب، ثم إطلاق مسبار PTO لإعادة إدراج الحزمة المفقودة في المجدول وإعادة إرسالها، والتأكد من تسليم الحزم كاملة بالتسلسل التام `[seq0..seq5]` مع حمل كل رسالة لرقم تسلسلها الحقيقي المستقل (FR-8).
- **عزل التزامن ومنع انسداد نقطة النهاية (Runtime ↔ Core ↔ Socket):**
  تشغيل عميلين متزامنين على خادم موحد، مع إيقاف استهلاك القناة لأحدهما تماماً حتى امتلاء مخزن 1024، والتأكد الفعلي من أن العميل الآخر يستمر في تبادل الرسائل بسرعة ودون أي تأخير أو فقد (Zero HoL).
- **حماية الموزع وأمن التعمية (Runtime ↔ Wire ↔ Crypto):**
  إرسال آلاف الحزم المشفرة التي يتعمد فيها بدء نص التشفير بقيم تطابق إطارات المصافحة (0x0B/0x0C/0x0D)، والتأكد من توجيهها كنص مشفر عادي وفكها بنجاح دون أي إسقاط (N-1).
- **إنصاف الجدولة تحت التشبع (Scheduler ↔ Core ↔ Wire):**
  إغراق دائم لحركة P1 مع تزويد حركة مستمرة لـ P3 و P4، وإثبات أن خوارزمية DRR المعدلة تضمن خدمة P3 و P4 بحصصهما العادلة دون تجويع (N-3).
- **هجرة المسار والتحقق العكسي (Path ↔ Core ↔ Runtime):**
  تأكيد كبت إرسال ACK إلى العناوين غير الموثقة، وإطلاق التحدي، واستقبال الرد، وترقية المسار النشط مع تصفير إحصائيات RTT وتحديث رصيد مكافحة التضخيم الخاص بالمسار الجديد فقط (New-8, New-12, X-1, A-5).

---

## 4. المراحل التنفيذية لخطة الإصلاح الشاملة

### المرحلة 1: دمج الفروع وتوحيد الإصلاحات الحرجة الجاهزة
1. تثبيت وحفظ التعديلات الحالية الخاصة بـ `New-12` في فرعها.
2. دمج فرع `fix/New-8-per-path-validation` (المشتمل على N-1، N-2، X-1، A-5، New-8) في فرع التكامل `integration/all-fixes`.
3. دمج كود `New-12` واختباراته الخمسة للتأكد من انطباق كامل حزمة الأمان والتوجيه.
4. تشغيل بوابة الفحص والتأكد من ارتفاع عدد الاختبارات الناجحة إلى أكثر من 117 اختباراً.

### المرحلة 2: إصلاح خوارزمية الجدولة وإلغاء التجويع (N-3, FR-7, N-7)
1. **تصحيح خوارزمية DRR (N-3):**
   - إضافة `current_round_index: usize` في `GameScheduler`.
   - تعديل دالة `pop_next` لتبدأ البحث من الطبقة الحالية وتتحرك دائرياً بين الطبقات النشطة، مع إضافة الرصيد للطبقة فقط عند زيارتها في الدورة الحالية.
   - إنشاء اختبار انحدار `test_drr_fairness_under_p1_saturation` للتأكد من حصول P3 و P4 على حصصهما المجدولة.
2. **تسريع حساب بايتات الطبقات O(1) (FR-7):**
   - استبدال المسح التكراري `sum()` بمصفوفة عدادات تراكمية `queue_bytes: [usize; NUM_PRIORITY_TIERS]` يتم تحديثها عند كل إدخال وإخراج.
3. **تقييد عدد العناصر بالطوابير (N-7):**
   - إضافة حد أقصى لعدد العناصر في طوابير الجدولة ومخزن الترتيب لحماية النظام من إغراق الحزم الفارغة ذات الحجم صفر.

### المرحلة 3: إصلاح بروتوكول التعافي وسلامة البيانات (N-4, FR-8, N-5, FU-4, FU-5, FR-5)
1. **انضباط مسبار PTO مع RFC 9002 (N-4 / FU-4):**
   - تعديل مسار PTO في `connection.rs` بحيث لا يستدعي `cc.on_timeout` عند كل مسبار استطلاعي، وحصر خفض النافذة عند تحقق الازدحام المستمر (Persistent Congestion بعد 3 مسابر متتالية).
   - إزالة استدعاءات `cc.on_loss` الصورية ذات الأحداث الفارغة وتنظيف التعليقات القديمة.
2. **تصحيح تسلسل الحزم المسترجعة من مخزن الترتيب (FR-8):**
   - تعديل توقيع دالة `OrderedGroupReceiver::on_incoming` لتعيد أزواج `(u32, Vec<u8>)`.
   - تعديل حلقة الاستقبال في `connection.rs` لإسناد `order_seq` الفعلي لكل رسالة مستخرجة.
3. **معالجة القيمة الحارسة لـ min_rtt (N-5):**
   - تعديل `rtt.rs` و `calculate_backpressure` وواجهات المقاييس للتعامل السليم مع عدم وجود عينة RTT بعد، وعرض مؤشر واضح (`None` أو `n/a`) بدلاً من الرقم الخيالي `18446744073709s`.
4. **ترقية سياسة إخلاء المجموعات إلى LRU (FU-5):**
   - تحديث ترتيب المجموعة في طابور الإخلاء عند كل وصول إليها لحماية المجموعات النشطة من الإخلاء المبكر.
5. **إضافة بوابة حالة الاستقبال (FR-5):**
   - فحص حالة الاتصال في مقدمة `handle_incoming_datagram` والعودة فوراً إذا كان الاتصال في حالة `Closed`.

### المرحلة 4: أمان طبقة السلك والترميز والمفاتيح (WIR-2, WIR-4, SEC-14)
1. **تصحيح ترميز نطاقات ACK (WIR-2):**
   - كتابة `count as u8` في ترويسة الإطار بدلاً من القيمة الأصلية غير المقيدة لضمان التوافق التام مع المفكك.
2. **حماية نصوص UTF-8 في إطار Close (WIR-4):**
   - تقليم سبب الإغلاق عند حدود المحارف الصحيحة عبر `floor_char_boundary(255)`.
3. **حجب المفاتيح الخام في Debug (SEC-14):**
   - تنفيذ `Debug` مخصص يحجب أسرار التشفير في `DirectionalKeys` والمفاتيح المشابهة.

### المرحلة 5: تصحيح التوثيقات والـ CLI وإدارة Radicle
1. **تصحيح أمر Dissect وتوثيق README (N-6):**
   - تعديل مثال الـ Hex في `README.md:93` و `gtp-cli` ليكون متطابقاً مع طول الترويسة الطويلة (≥ 28 بايت).
   - توثيق مصافحة المسار كثنائية الاتجاه وتصحيح قيمة β الافتراضية إلى 0.75.
   - استبدال النصوص الثابتة غير المشروطة في `StressSuite` بفحوصات برمجية حقيقية.
2. **المزامنة عبر Radicle:**
   - الحفاظ على سير العمل عبر مستودع Radicle المحلي وتوثيق التغييرات دون أي اتصال أو رفع إلى GitHub.

---

## 5. مصفوفة الإغلاق النهائي والتحقق

| الإجراء | المعايير المستهدفة | أداة التحقق | النتيجة المتوقعة |
| :--- | :--- | :--- | :---: |
| دمج الفروع وإصلاح N-1/N-2 | خلو داتاغرامات البيانات من الإسقاط، وانعدام HoL في RX | `cargo test -p gtp-runtime-tokio` | ✅ 100% نجاح |
| إصلاح خوارزمية الجدولة N-3 | عدالة توزيع الحصص بين الطبقات ومنع تجويع P3/P4 | `test_drr_fairness_under_p1_saturation` | ✅ P3/P4 > 0% |
| انضباط PTO وفق RFC 9002 | ثبات cwnd عند مسابر PTO الفردية | `test_pto_probe_does_not_collapse_cwnd` | ✅ cwnd ثابت |
| تصحيح أرقام تسلسل FR-8 | كل رسالة مستلمة تحمل رقم تسلسلها الصحيح | `test_ordered_group_drain_preserves_distinct_order_seq` | ✅ تسلسل دقيق |
| تصحيح عيوب Wire و Crypto | سلامة الترميز وحجب المفاتيح في السجلات | `cargo test -p gtp-wire -p gtp-crypto` | ✅ 100% نجاح |
| تشغيل بوابة الجودة الكاملة | فحص fmt و clippy وجميع الاختبارات والفحوص الإجرائية | `bash scripts/verify_remediation.sh` | ✅ **GATE: PASSED** |

---
*تم اعتماد وتخزين هذه الخطة كمرجع تشغيلي وهندسي دائم للمشروع في مسار التطوير عبر Radicle.*
