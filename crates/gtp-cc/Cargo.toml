[package]
name = "gtp-cc"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
gtp-types = { workspace = true }
gtp-recovery = { workspace = true }

[features]
default = ["std"]
std = ["gtp-types/std", "gtp-recovery/std"]
