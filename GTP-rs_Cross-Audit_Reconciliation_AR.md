# GTP-rs — مطابقة التدقيقين والتحقق المستقل من النتائج الجديدة

**المستودع:** `e1c9ef7` — النسخة المحلية `/home/ggonlinux/GTP/`
**الوثائق المقارَنة:**
- (أ) `GTP-rs_Technical_Audit_and_Optimization_Plan_AR.md` — التقرير الأول
- (ب) `GTP-rs-Architecture-Protocol-Audit-Paper-v1_0.md` — الورقة الجديدة v1.0
- (ج) `GTP-rs_Deep_Audit_Verified_AR.md` — تدقيقي السابق

**التاريخ:** 30 أغسطس 2026

---

## 0. الحكم على الورقة الجديدة (ب)

الورقة (ب) **أقوى وأشمل من (أ) ومن (ج) مجتمعتين** من حيث التغطية: 6 عيوب حرجة + 17 عالياً + 34 متوسطاً، بمواضع `file:line` دقيقة، ومع تشغيل فعلي لـ `cargo test` (48 ناجحاً) وهو ما لم أستطع فعله (سلسلة أدوات Rust غير متاحة في بيئتي).

الأهم: **وصلَت الورقة (ب) وتدقيقي (ج) إلى الأطروحة المركزية نفسها بشكل مستقل** — «الطبقات سليمة داخلياً والعيوب كلها على نقاط الالتحام بينها، وهي بالضبط ما لا تغطيه الاختبارات». تطابق استنتاجين مستقلين على هذه النقطة يرفع الثقة بها كثيراً، ويجعلها المحور الصحيح لخطة الإصلاح.

**النتيجة الصافية للمقارنة:**
- **21 عيباً في (ب) لم أتطرق إليها** — تحققتُ من 13 منها مباشرة في الشيفرة: **كلها صحيحة**، واثنان منها (SEC-1، Core-C1) أخطر من أي شيء ورد في تدقيقي.
- **عيبان جوهريان عندي غائبان عن (ب)** — أحدهما (عطب حساب نطاقات ACK) **تصنّفه الورقة (ب) صراحةً ضمن «الصحيح»**، وهو خطأ في الورقة أعدتُ التحقق منه ثلاث مرات.
- **موضع واحد يحتاج تصحيحاً تقنياً في (ب)** (آلية CC-3) — والتصحيح يكشف عيباً ثالثاً أكثر يقيناً لم ترصده أي وثيقة.

---

## 1. ✱ الاكتشاف الأخطر في الملف كله: إعادة استخدام nonce بين الاتجاهين (SEC-1)

**هذا أخطر ما في المستودع، وقد فاتني تماماً. تحققتُ منه مباشرة وهو صحيح بالكامل.**

سلسلة التحقق التي نفّذتها:

```
1) grep على crates/gtp-crypto + state.rs + endpoint.rs بحثاً عن
   role / direction / client_tx / server_tx / is_client  →  صفر نتيجة
2) العميل  (endpoint.rs:183): GtpConnection::new_with_session_keys(cid, .., key, iv, true, ..)
3) الخادم  (endpoint.rs:408): GtpConnection::new_with_session_keys(cid, .., key, iv, true, ..)
   ← نفس (key, iv) الناتجين عن derive_handshake_session_keys
4) state.rs:59  →  next_packet_number: PacketNumber(1)  للطرفين
5) aead.rs:21   →  nonce = IV ⊕ (CID_be ‖ PN_be[4..8])   دالة حتمية بحتة
```

⇒ **حزمة العميل رقم 1 وحزمة الخادم رقم 1 تستخدمان المفتاح نفسه والـ nonce نفسه.** وكل رقم حزمة N يتصادم بين الاتجاهين طوال الجلسة.

**لماذا هذا أخطر من ملاحظتي في (ج §3.8):** رصدتُ تكرار الـ nonce عند 2³² حزمة — أي بعد أيام من التشغيل المتواصل. الورقة (ب) رصدت أن التصادم يقع عند **الحزمة الأولى**، في كل جلسة، بلا استثناء.

**الأثر التقني الكامل** (ChaCha20-Poly1305 / RFC 8439):
1. **انهيار السرية:** `C₁ ⊕ C₂ = P₁ ⊕ P₂`. حركة الألعاب شديدة البنيوية والتوقّع (ترويسات إطارات ثابتة، معرّفات متسلسلة، إحداثيات ضمن مدى ضيق) ⇒ استرجاع النص الصريح عملي.
2. **انهيار السلامة — وهو الأخطر:** مفتاح Poly1305 لمرة واحدة يُشتق من (key, nonce) فقط. تصادم الـ nonce يكشفه ⇒ **تزوير حزم مصادَق عليها بالكامل**. اختلاف الـ AAD بين الاتجاهين (الطوابع الزمنية) **لا يحمي**: الـ AAD يدخل Poly1305 فقط ولا يمسّ keystream إطلاقاً.
3. المهاجم لا يحتاج أي مفتاح — يحتاج فقط مراقبة الاتجاهين، وهو ما يتوفر لأي وسيط شبكي.

**تفصيل إضافي أؤكده:** الاختبار في `handshake.rs:155-163` يحتوي `assert_eq!(client_key, server_key)` — أي أن **الحالة الخطرة مثبَّتة في المجموعة الاختبارية كسلوك مطلوب**. أي إصلاح صحيح سيجعل هذا الاختبار يفشل، وهذه إشارة نجاح لا فشل.

**الإصلاح (كما في (ب) 0.1، وأضيف تفصيله):**

```rust
// في derive_handshake_session_keys: اشتق أربع مواد لا اثنتين
let (c2s_key, c2s_iv) = expand(&master, b"gtp/v1 client->server");
let (s2c_key, s2c_iv) = expand(&master, b"gtp/v1 server->client");
// كل طرف: يختم بـ tx_key ويفتح بـ rx_key حسب دوره
```

واختبار إلزامي: `assert_ne!(c2s_key, s2c_key)` + إثبات أن فك حزمة العميل بمفتاح العميل **يفشل**.

---

## 2. ✱ الاكتشاف الثاني الذي فاتني: إطارات التحكم مغلّفة مرتين (Core-C1)

**تحققتُ منه مباشرة في `control/handle.rs` — صحيح بالكامل، والسلسلة أوضح مما وصفتُه في (ج §3.16).**

كل دوال التحكم (`set_ack_frequency`, `trigger_path_challenge`, `trigger_mtu_probe`, `send_ping`, `graceful_close`) تتبع النمط نفسه حرفياً:

```rust
let frame = Frame::PathChallenge { data: nonce };
let mut buf = [0u8; 16];
let written = frame.encode(&mut buf)?;          // ترميز إطار كامل
let item = SchedulableItem {
    class: MessageClass::Unreliable,            // ← يُلَف لاحقاً في Frame::Data
    payload: buf[..written].to_vec(),           // بايتات الإطار كـ «حمولة تطبيق»
    ...
};
```

ومسار TX (`connection.rs:576-586`) يلفّ **كل** عنصر `Unreliable` داخل `Frame::Data`. النتيجة: النظير يستلم إطار `Data` حمولته بايتات إطار تحكم خام، **فيسلّمها إلى التطبيق كرسالة لعبة**. معالِجات `PathChallenge`/`PathResponse`/`Close`/`AckFrequency`/`Ping` في `handle_incoming_datagram` **لا تنطلق أبداً** لأي حركة محلية المنشأ.

**التتويج الذي رصدته الورقة وأؤكده:** `graceful_close` ينقل الحالة إلى `Draining` **قبل** الإرسال، بينما بوابة TX هي:

```rust
if !self.hot.state.is_active() && !matches!(self.hot.state, Handshaking) { return Ok(None); }
```

و`is_active()` تعني `Established` فقط ⇒ **الإغلاق المهذب لا يرسل بايتاً واحداً على الإطلاق**.

**هذا يصحّح ويعمّق ملاحظتي في (ج §3.16):** قلتُ إن ترحيل المسار ميت لأن `start_challenge` لا يُستدعى من مسار الاستقبال. الصحيح أن العطب أعمق: حتى الاستدعاء الصريح عبر `trigger_path_challenge` — الذي **يستدعي** `start_challenge` فعلاً — لا يصل التحدي إلى النظير مطلقاً بسبب التغليف المزدوج. والرد التلقائي في `connection.rs:389` (`b[..9].to_vec()` داخل عنصر `Unreliable`) مصاب بالعطب نفسه. لاحظتُ ذلك الاختراق في الشيفرة لكنني لم أتتبّع نتيجته حتى نهايتها.

**الميزات الميتة طرفاً-لطرف:** ترحيل المسار، NAT rebinding، الإغلاق المهذب، Ping/keepalive، اكتشاف PMTU، تفاوض تردد ACK. جميعها **مُوثَّقة ومُختبرة وحدة-بوحدة** وكلها معطّلة عملياً.

---

## 3. بقية اكتشافات (ب) الجديدة عليّ — نتيجة التحقق

تحققتُ مباشرة من العناصر المؤشَّرة بـ ✅؛ والباقي مُتَّسق بنيوياً مع ما قرأتُه ولم أفحص سطره بالتحديد.

| المعرف | الادعاء | التحقق |
| :--- | :--- | :--- |
| **SEC-7** | `x25519-dalek` بـ `default-features = false` ⇒ ميزة `zeroize` معطّلة، المفاتيح الخاصة لا تُمحى | ✅ **صحيح** — `Cargo.toml:50` مؤكَّد. مفارقة لافتة: `handshake.rs` يستخدم `ZeroizeOnDrop` بعناية على `HandshakeSharedSecret` بينما المفتاح الخاص نفسه لا يُصفَّر. (يعطّل أيضاً `precomputed-tables` ⇒ خسارة أداء في DH) |
| **SEM-1** | `GenerationId::is_newer_than` بـ `>` عادية بينما `StateSequence` تنفّذ RFC 1982 صحيحاً في **الملف نفسه** | ✅ **صحيح** — `identifiers.rs:141-144` مقابل `:170-172`. نظاما ترتيب متناقضان لعدّادين يسافران في الإطار ذاته. لم أرصده |
| **SEC-11** | مقيّد معدل ClientHello: التعليق «20/ثانية» والكود `entry.0 <= 1000` | ✅ **صحيح** — `endpoint.rs:290`. فارق ×50 عن النية المُوثَّقة |
| **SEC-10** | فيض في `payload.len() < payload_len + AEAD_TAG_LEN` | ✅ **صحيح** — `aead.rs:45`. الحل: `checked_add`. غير قابل للتفعيل عن بُعد (القيمة داخلية) لكنه ذعر في واجهة عامة |
| **WIR-1** | النمط نفسه في `offset + 4 + *padding_len` لـ `MtuProbe`/`Padding` | ✅ **صحيح** — `frame.rs:378`. الحقول عامة `usize` |
| **CORE-3** | حلقة RX `while let Ok(..)` تموت عند أول خطأ socket | ✅ **صحيح** — `endpoint.rs:247`. **وأشدّده:** على لينكس/ويندوز يعيد سوكِت UDP خطأ `ConnectionReset`/`ECONNREFUSED` عند وصول ICMP port-unreachable ⇒ **طرف بعيد يستطيع قتل الـ endpoint بأكمله** (كل الاتصالات) بحزمة ICMP واحدة. الورقة تصنّفه «عالياً»؛ أراه أقرب للحرج تشغيلياً |
| **WIR-12** | حزمة `fuzz` خارج أعضاء الـ workspace ⇒ لا تُبنى ولا تُشغَّل | ✅ **صحيح** — `fuzz/Cargo.toml` package مستقل، و`Cargo.toml` الجذر لا يذكره. رصدتُ في (ج) أن CI لا يشغّل الـ fuzz؛ الورقة أضافت أنه لا يُبنى أصلاً فقد يكون متعفناً |
| **WIR-10** | `endpoint.rs:113` يرسل `version = 1` بينما `GTP_V1_1 = 0x00010001`، ولا تحقق من النسخة في أي مسار | ✅ **صحيح** — تنفيذان داخليان غير متفقين، ولا أحد يلاحظ لأن الحقل لا يُقرأ |
| **gtp-io ميت** | `PacketIo` غير مستورد من أي مسار حي | ✅ **صحيح** — معلن كتبعية في 3 حزم، والاستخدام الوحيد `pub use gtp_io as io` في الواجهة. لا `sendmmsg`/`recvmmsg`/GSO |
| **REC-6** | سقف 32 فترة يُسقط الإقرارات بصمت بلا دمج | ✅ **صحيح** — `take(MAX_ACK_RANGES - 1)`. يكمّل ملاحظتي (ج §3.10): أنا رصدتُ عدم التقليم، والورقة رصدت أن السقف يسبب **إعادة إرسال قسرية عبر PTO** لحزم وصلت فعلاً |
| **REC-8** | إطار ACK في **كل** datagram صادر — يتجاوز التكيّف كلياً | ✅ **صحيح** — الشرط `should_ack \|\| has_queued_data` و`generate_ack_frame` تعيد `Some` كلما وُجد `largest_received`. **مهم جداً للقسم 4 أدناه** |
| **REC-7** | `ack_frequency`/`max_ack_delay` صلبة (2 و25ms) ولا تُغذّى من `GtpConfig`؛ إطار AckFrequency الوارد يعدّل إعداداً لا يقرؤه أحد ⇒ التفاوض no-op | ✅ **صحيح بنيوياً** — `AckTracker::default()` يثبّتهما، و`connection.rs:423-432` يكتب في `self.config` فقط |
| **CC-6** | كل معاملات CC في `GtpConfig` ميتة (تُنشأ الوحدات بـ `Default`) | ✅ **صحيح** — `state.rs` ينشئ `CubicCongestionController::default()` و`PacingEngine::default()` بلا تمرير الإعداد. الإعدادات المسبقة `competitive_fps()`/LAN/Mobile تجميلية في شقّها الخاص بالـ CC |
| **ORD-4** | `ReliableUnordered` (group 0) يُسلَّم بلا إزالة تكرار | ✅ **صحيح** — لا يوجد أي تتبّع لـ `message_id` مستلمة. رصدتُ الخطر في `Frame::Retx` ولم أعمّمه على المسار الرئيسي |
| **ORD-3** | `let _ = scheduler.enqueue(item)` لإعادة إرسال «موثوقة» ⇒ فقدان صامت عند امتلاء P3 | ✅ **صحيح** — موضعان (RX:326، PTO:525) وكلاهما `let _`. يكمّل (ج §3.12) بمسار فقدان ثالث لم أرصده |
| **PATH-1** | `Initial → Closed` غير مسموح ⇒ إغلاق اتصال فشلت مصافحته مستحيل | ✅ **صحيح** — أشرتُ إليه عرضاً في (ج) دون استخلاص النتيجة العملية |
| **WIR-6** | `header_len` يُكتب ولا يُحترم؛ ترويسة بـ `header_len=100` تجعل المستقبل يقفز 76 بايت من الإطارات كـ«امتدادات» صامتة | ✅ **صحيح** — لاحظتُ في (ج §3.20) أن `header_len` مُتحكَّم به من النظير، والورقة استخرجت السيناريو العملي كاملاً |
| **SEC-9** | إثبات HMAC يعيد استخدام مفتاح AEAD نفسه (خلط أدوار) | ✅ **صحيح** — `compute_client_proof(&key, ...)` حيث `key` هو مفتاح AEAD. يجب اشتقاق `finished_key` منفصل |
| **CC-2** | النافذة تنمو على ACK مكرر/فارغ (`bytes_acked = 0`) | ✅ **صحيح** — `on_ack` تستدعي `update_w_cubic` بلا بوابة على `bytes_acked > 0` |
| **CC-1** | `on_timeout` لا يصفّر `k`/`origin_point`/`w_max` ⇒ `max()` يستعيد النافذة القديمة فوراً | ✅ **صحيح** — رصدتُ سقّاطة `max()` في (ج §3.15)، والورقة ربطتها بـ §4.7 من RFC 8312 وبيّنت أن slow start بعد المهلة مُفرَّغ عملياً |
| **ORD-2** | امتلاء مخزن المجموعة يُجهض الـ datagram **بما فيه إطارات ACK** ويتخطى `ack_tracker.on_packet_received` ⇒ الحزمة لا يُعترف بها فتُعاد وتفشل بالطريقة نفسها = **livelock** | ✅ **صحيح** — رصدتُ الإجهاض في (ج §3.18)؛ الورقة استخرجت حلقة الـ livelock وهي الاستنتاج الأهم |
| **CORE-4/5** | جدول CID بلا إخلاء + `.await` للتسليم داخل حلقة RX الوحيدة (HOL على مستوى الـ endpoint) | ✅ متسق مع بنية `endpoint.rs` |

---

## 4. ✱ العيب الذي تصنّفه الورقة (ب) «صحيحاً» وهو معطوب

**هذه أهم مساهمة متبقية من تدقيقي، وأعرضها بحذر لأنها تناقض حكماً صريحاً في الورقة (ب).**

الورقة تُدرج في §6.2 تحت عنوان **«الصحيح»**:

> «مجموعة فترات مرتبة تنازلياً مع دمج المجاورة؛ **دلالات ترميز الفترات مطابقة لـ QUIC**»

المُرمِّز فعلاً مطابق دلالياً لـ QUIC (بإزاحة ±1 في اصطلاح `gap`). **لكن الورقة فحصت المُرمِّز ولم تفحص المُفكِّك.** والمُفكِّك في `loss_detector.rs` يستهلك `gap` في الترتيب المعاكس:

```rust
// المُرمِّز — gap_i يصف الفجوة السابقة للنطاق i
let gap = prev_start.saturating_sub(interval.end + 1);

// المُفكِّك — يستخدم النطاق i أولاً ثم يطرح gap_i
let start = current_pn.saturating_sub(range.length as u64);   // ← قبل طرح الفجوة
for pn in (start..=current_pn).rev() { acked_numbers.push(pn); }
current_pn = current_pn - range.length - range.gap - 1;       // ← الطرح متأخر خطوة كاملة
```

المُفكِّك مكتوب كأن `gap_i` يصف الفجوة **التالية** للنطاق i، والمُرمِّز يكتبه كفجوة **سابقة** له. النصفان يستخدمان اصطلاحين متعاكسين. النتيجة صحيحة فقط عندما `gap = 0` لكل النطاقات — أي عند غياب الفقد تماماً، وهو بالضبط ما تغطيه الاختبارات الحالية (`range_count == 1`).

**إعادة التحقق العددي** (المستقبِل استلم `1..5`, `8..10`, `20..22`):

```
النطاقات المُرمَّزة (gap,len): [(0,2), (9,2), (2,4)]
الحقيقة          : 1 2 3 4 5 | 8 9 10 | 20 21 22
ما يفكّه المرسِل  : 3 4 5 6 7 | 17 18 19 | 20 21 22

أُقرّت زوراً (لم تصل قط): 6, 7, 17, 18, 19
فُقد إقرارها (وصلت فعلاً): 1, 2, 8, 9, 10
```

الحزم المُقرّة زوراً تُحذف من `sent_packets` وتُعدّ مُسلَّمة ⇒ **حمولاتها الموثوقة لا تُعاد إرسالها أبداً**.

### التركيب الذي يجعل هذا العيب دائماً لا عرضياً

هنا تلتقي ثلاث نتائج من الوثيقتين معاً، والتركيب أخطر من أي منها منفردة:

| المصدر | النتيجة |
| :--- | :--- |
| (ب) REC-5 | فترات ACK **لا تُقلَّم أبداً** |
| البنية | إعادة الإرسال تستخدم **أرقام حزم جديدة** ⇒ فجوة أي حزمة مفقودة **دائمة ولا تُملأ** |
| (ب) REC-8 | إطار ACK يُلحَق بـ **كل** datagram صادر |
| (ج) هذا العيب | الترميز/الفك معطوب كلما وُجدت فجوة (≥ 2 فترة) |

**الخلاصة المركّبة:** بمجرد ضياع **حزمة واحدة**، يصبح عدد الفترات ≥ 2 ولا ينخفض أبداً، وكل إطار ACK يُرسَل بعد تلك اللحظة — أي **كل حزمة صادرة لبقية عمر الاتصال** — يحمل إقرارات خاطئة. العيب ليس حالة حدّية بل يتحول إلى الوضع الدائم بعد أول فقد.

هذا هو السبب الجذري الذي يجعل «قناة موثوقة» تفقد الرسائل بصمت تحت الفقد، وهو ظرف وجودها بالذات.

**اختبار الإثبات** (يفشل الآن قطعاً — موجود بنصه الكامل في تدقيقي السابق §7-أ): بناء `AckTracker` باستلام `1..5, 8..10, 20..22`، توليد إطار ACK، تمريره لـ `LossDetector`، وتأكيد أن مجموعة الحزم المُقرّة تساوي المجموعة المستلمة بالضبط.

---

## 5. عيوب أخرى عندي غائبة عن (ب)

| # | العيب | الحالة في (ب) |
| :--- | :--- | :--- |
| 1 | **عطب حساب نطاقات ACK** (§4 أعلاه) | مُصنَّف «صحيح» — الاختلاف الجوهري الوحيد |
| 2 | **هجوم ACK المتفائل**: لا فحص `largest_acked ≤ largest_sent`. نظير يُقرّ حزماً لم تُرسل قط ⇒ نفخ `cwnd` وكسر التحكم بالاحتقان | REC-10 يعالج استنزاف الذاكرة من `length` الكبيرة، لكن لا يذكر نفخ النافذة. **متمم لا متعارض** — الإصلاح واحد: تحقق دلالي من الإطار |
| 3 | **عدم التعافي بعد black-hole**: لا كشف فقد بمؤقت + `cc.inflight` لا يُصرَّف ⇒ `send_budget = cwnd − inflight = 0` **حتى بعد عودة المسار** | REC-12 يذكر غياب آلة المؤقتات وCC-3 يذكر تسريب inflight، لكن لم تُستخلص نتيجة «الاتصال لا يتعافى». تكامل لا تعارض |
| 4 | **توصية حماية الترويسة (header protection)**: الـ CID والـ PN بنص صريح هما ما يجعل هجوم SEC-3 ممكناً عن بُعد وبكلفة صفرية، ويسمحان بتتبّع الجلسات (linkability) | (ب) ترصد SEC-3 بدقة لكنها لا تربطه بغياب تشفير الترويسة ولا تقترحه كإصلاح جذري. إضافة معمارية مفيدة |

---

## 6. تصحيح تقني واحد على الورقة (ب) — وعيب ثالث يكشفه

### CC-3 — الآلية الموصوفة غير دقيقة، والواقع أسوأ من زاوية أخرى

**ادعاء (ب):** بايتات الحزم ACK-only تُقيَّد في `cc.inflight`، والنظير «لا يعترف بها بتصميمه» ⇒ نمو **رتيب** حتى استهلاك `cwnd − inflight` وتوقف الإرسال، وسجلاتها تتراكم «للأبد» في `sent_packets`.

**ما تحققتُ منه:** الجزء الأول صحيح تماماً — `cc.on_packet_sent(pn, total_datagram_len, now)` يُستدعى **بلا شرط** (`connection.rs:689`) حتى حين `ack_eliciting == false`، بينما السجل يحمل `in_flight: ack_eliciting` أي `false`.

**لكن** حلقة كشف الفقد في `on_ack_received` تمرّ على **كل** `sent_packets` بلا ترشيح على `in_flight`:

```rust
for (&pn, record) in &self.sent_packets {
    if pn > largest_pn { continue; }
    if largest_pn >= pn + PACKET_THRESHOLD || time_threshold_exceeded { lost_pns.push(pn); }
}
```

فسجلات ACK-only **تُحذف فعلاً** كـ«مفقودة»، وتدخل في `bytes_lost`، و`cc.on_loss` يخصمها من `inflight`. إذن:

- النموّ الرتيب حتى التوقف **يتحقق فقط عند توقف وصول الـ ACKs** — لا في الحالة العامة كما تصف الورقة.
- التراكم «للأبد» في `sent_packets` **غير دقيق** في المسار الطبيعي.

**والأهم — العيب الذي يكشفه هذا التصحيح ولم ترصده أي وثيقة:**

> **كل حزمة ACK-only تُعلَن لاحقاً «مفقودة» زوراً، فتستدعي `cubic.on_congestion_event` وتُخفّض `cwnd` بالمعامل β = 0.7 بلا أي فقد حقيقي.**

الأثر عملياً أشد من التسريب: في **عميل لعبة نموذجي** — يستقبل تدفق حالة العالم ويرسل مدخلات قليلة ⇒ نسبة عالية من حزمه ACK-only — تتكرر التخفيضات الزائفة باستمرار. الكابح الوحيد هو شرط «مرة واحدة لكل RTT» في `on_congestion_event`، أي **تخفيض زائف واحد كل RTT بشكل دائم** ⇒ `cwnd` محبوسة عند `min_cwnd = 2×SMSS` عملياً مهما كانت الشبكة نظيفة.

**الإصلاح يعالج الثلاثة معاً:** استثناء الحزم ACK-only من محاسبة `inflight` في الـ CC، **و** ترشيح حلقة كشف الفقد على `record.in_flight == true` (وهو ما يوجبه RFC 9002 §2 أصلاً: لا يُعلَن فقد إلا لحزم in-flight).

### تصحيحات صغيرة أخرى على (ب)

| الموضع | الملاحظة |
| :--- | :--- |
| §3.2، جدول الترويسة | `connection_id` مُدرج بحجم **10 بايت** عند الإزاحة 2 — الصحيح **8** (الإزاحات: 0، 1، 2..10، 10..18، 18..22، 22..24). خطأ طباعي في الجدول لا في التحليل |
| REC-10 «~34GB» | الرقم صحيح لفترة واحدة بـ `length = u32::MAX` (‏4.29×10⁹ × 8 بايت). عملياً تموت العملية عند أول فترة، فالتقدير هو الأدق للاستخدام |
| WIR-1 تصنيف | التصنيف المزدوج (عالٍ كمكتبة / منخفض كخدمة) دقيق وأتفق معه — مسار الفك لا يولّد `usize` مطلقاً |

---

## 7. القائمة الحرجة الموحّدة بعد الدمج

| # | العيب | المصدر | الأثر |
| :--- | :--- | :--- | :--- |
| **1** | إعادة استخدام nonce بين الاتجاهين من الحزمة الأولى | (ب) SEC-1 | انهيار السرية **والسلامة** — تزوير حزم مصادَقة بلا معرفة أي مفتاح |
| **2** | عطب ترميز/فك نطاقات ACK ⇒ إقرار زائف ⇒ فقد دائم صامت على القناة الموثوقة، **دائم بعد أول فقد** | (ج) + تركيب مع (ب) REC-5/REC-8 | القناة الموثوقة غير موثوقة تحت الفقد |
| **3** | إطارات التحكم مغلّفة مرتين + الإغلاق المهذب لا يُرسل | (ب) Core-C1 | 6 ميزات معلنة ميتة طرفاً-لطرف |
| **4** | نافذة إعادة التشغيل تُحدَّث قبل مصادقة AEAD | (أ) + (ب) SEC-3 + (ج) | قطع اتصال دائم بحزمة UDP واحدة من خارج المسار |
| **5** | كوكي XOR ⇒ استرجاع 24 بايت من السرّ من كوكي واحدة | (أ) + (ب) SEC-4 + (ج) | انهيار التحقق من العنوان ومقاومة DoS |
| **6** | مصافحة مجهولة الهوية + لا فحص `was_contributory` | (ب) SEC-5/12 + (ج) | MITM نشط كامل |
| **7** | تخصيص يتحكم به الخصم من إطار ACK (~34GB) + لا فحص `largest_acked ≤ largest_sent` | (ب) REC-10 + (ج) | إسقاط العملية / نفخ cwnd من نظير واحد |
| **8** | PTO بلا إزالة سجلات ولا تراجع أُسّي ولا حد + `ReliableUnordered` بلا إزالة تكرار | (أ) + (ب) REC-11/ORD-4 + (ج) | عاصفة تصاعدية + تسليم مكرر للتطبيق |
| **9** | امتلاء المجموعة يُجهض datagram بما فيه ACKs ⇒ livelock | (ب) ORD-2 | حلقة إعادة إرسال لا تنتهي |
| **10** | ratchet غير منسّق (KEY_PHASE بلا استخدام) ⇒ استدعاؤه يُعطّل الاتصال | (ب) SEC-6 + (ج) | ميزة معلنة تُبرِك الاتصال |
| **11** | كل حزمة ACK-only تُعلَن مفقودة زوراً ⇒ تخفيض cwnd كل RTT | **جديد — هذا التقرير §6** | `cwnd` محبوسة عند الحد الأدنى في العملاء المستقبِلين |
| **12** | موت RX الصامت للـ endpoint عند أول خطأ socket (ICMP) | (ب) CORE-3 | طرف بعيد يجمّد كل الاتصالات على العقدة |

---

## 8. ترتيب التنفيذ المُوصى به

خطة المرحلة 0 في الورقة (ب) ممتازة وأتبنّاها كما هي، مع **إضافتين وإعادة ترتيب واحدة**:

**المرحلة 0 (فوري) — إضافة إلى بنود (ب) 0.1–0.6:**
- `0.7` **تصحيح المُفكِّك في `loss_detector.rs`** ليطرح `gap` قبل حساب `start`، مع اختبار خصائص (property test) لـ round-trip على مجموعات فجوات عشوائية. **هذا البند يجب أن يسبق كل قياس أداء لاحق** — أي رقم p99 أو goodput يُقاس قبله يُقاس على بروتوكول يفقد البيانات.
- `0.8` ضمّ فحص `largest_acked ≤ largest_sent` إلى بند `0.6` (تحقق دلالي من إطار ACK) — إصلاح واحد يغلق ثغرتين.

**المرحلة 1 — إضافة إلى (ب) 1.3:**
- ترشيح حلقة كشف الفقد على `record.in_flight == true` (RFC 9002 §2) بالتوازي مع استثناء ACK-only من `cc.inflight` — يعالج التخفيض الزائف للنافذة (§6 أعلاه) الذي لا يعالجه استثناء الـ inflight وحده.

**المرحلة 2 — رفع أولوية:**
- `CORE-3` (معالجة أخطاء السوكِت) من «عالٍ» إلى صدارة المرحلة 2: ناقل ICMP يجعله قابلاً للتفعيل عن بُعد على كل الاتصالات دفعةً واحدة.

**المرحلة 4** (بنية اختبار الالتحام) — أتفق تماماً مع خلاصة الملحق (د): هذه هي أعلى عائد استثمار في المشروع. وأضيف إلى قائمتها السيناريو المفقود:

> **11.** ACK تحت فجوات متعددة: توليد نمط استلام بفجوات عشوائية ثم تأكيد أن مجموعة الحزم التي يعتبرها المرسِل مُقرّة تساوي المجموعة المستلمة بالضبط — يكشف العيب رقم 2 في الجدول أعلاه.

---

## 9. خلاصة

الورقة (ب) هي الآن **المرجع الأساسي** للمشروع: أوسع تغطية، مواضع دقيقة، تحقق تجريبي فعلي، وخطة معالجة قابلة للتنفيذ. اكتشافاها SEC-1 وCore-C1 يتجاوزان في خطورتهما كل ما ورد في تدقيقي، وقد تحققتُ منهما مباشرة وأؤكدهما بلا تحفظ.

المساهمات المتبقية من طرفي ثلاث:
1. **عطب حساب نطاقات ACK** — تصنّفه الورقة «صحيحاً» وهو معطوب، وتركيبه مع REC-5/REC-8 يجعله دائماً بعد أول فقد.
2. **التخفيض الزائف للنافذة من حزم ACK-only** — يكشفه تصحيح آلية CC-3، ولم ترصده أي وثيقة.
3. **إضافات متممة**: هجوم ACK المتفائل، عدم التعافي بعد black-hole، وتوصية حماية الترويسة.

الأطروحة المشتركة تبقى الأدق وصفاً للحالة: **الطبقات سليمة، والدرزات بينها مكسورة، والاختبارات مصمَّمة بحيث لا تمرّ بالدرزات.** كل عيب من العيوب الاثني عشر أعلاه يقع على حدّ بين مكوّنين — بما فيها اكتشافاتي: مُرمِّز ACK ↔ مُفكِّك ACK، ومحاسب الـ CC ↔ كاشف الفقد.

**أوامر التحقق في بيئتك المحلية** (`/home/ggonlinux/GTP/`):

```bash
cd /home/ggonlinux/GTP
~/.rustup/toolchains/1.85.0-x86_64-unknown-linux-gnu/bin/cargo test --workspace --all-targets
~/.rustup/toolchains/1.85.0-x86_64-unknown-linux-gnu/bin/cargo clippy --workspace --all-targets -- -D warnings
# ضمّ fuzz إلى الـ workspace أولاً (WIR-12) ثم:
cargo +nightly fuzz run decode_frame -- -max_total_time=600
```
