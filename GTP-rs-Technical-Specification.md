# GTP-rs — المواصفة التقنية التفصيلية لفريق التطوير

**الوثيقة:** مواصفة تطوير ومتطلبات قابلة للتنفيذ والمتابعة
**الإصدار:** 1.0 — 31 أغسطس 2026
**الجمهور:** فريق تطوير GTP-rs
**الأساس التقني:** فرع `main` (`235089f`) + PR #1 (R-1…R-8) + PR #2 (المصافحة) — قيد المراجعة
**المرجعيات:** RFC 9000/9002 (النقل)، RFC 8312 (CUBIC)، RFC 1982 (حساب التسلسل)، وتقارير هذا العمل في `fixes/`

---

## 0. كيف تُقرأ هذه الوثيقة

كل متطلب يحمل **معرّفاً فريداً** (`FR-x` وظيفي، `SEC-x` أمني، `PERF-x` أداء، `TEST-x` اختبار)، و**أولوية** (P0 حرج … P3 منخفض)، و**معايير قبول** قابلة للقياس، و**اعتماديات**. مصفوفة التتبع في §12 تربط كل متطلب باختباره وحالته، لتكون المتابعة آلية.

**رموز الحالة:** ✅ منفَّذ ومُتحقَّق | 🔧 منفَّذ بلا اختبار | 🟡 مطلوب (مفتوح) | ⛔ محجوب باعتمادية.

---

## 1. المقدمة والنطاق

### 1.1 الغرض
GTP/1.1 (Game Transport Protocol) بروتوكول نقل فوق UDP مصمَّم لألعاب الوقت الحقيقي: يوفّر أربع دلالات تسليم على اتصال واحد مشفّر، مع تحكّم احتقان حديث ومصافحة X25519. هذه الوثيقة تحدّد **العمل المتبقّي** لبلوغ جاهزية الإنتاج، وتوثّق **الآلية والقواعد والمعايير** اللازمة لتنفيذه ومتابعته.

### 1.2 النطاق
- **داخل النطاق:** استكمال الثغرات الوظيفية والأمنية والأدائية المرصودة (§6–§9)، وبناء طبقة اختبار الالتحام (§10)، وخطة التنفيذ والمتابعة (§11).
- **خارج النطاق:** إعادة تصميم البروتوكول، تغيير صيغة السلك جذرياً، أو ميزات جديدة غير مذكورة هنا.

### 1.3 تعريفات
| المصطلح | المعنى |
| :--- | :--- |
| الدلالة (Class) | صنف التسليم: Unreliable / UnreliableSequenced / ReliableUnordered / ReliableOrdered |
| الدرزة (Seam) | نقطة الانتقال/التكامل بين طبقتين — موضع كل المخاطر المرصودة |
| CID | معرّف الاتصال (`ConnectionId`) |
| PTO | مهلة مسبار الإرسال (Probe Timeout، RFC 9002) |
| SMSS | أقصى حجم مقطع للمُرسِل (Sender Maximum Segment Size) |

---

## 2. نظرة عامة على المعمارية

### 2.1 النموذج الطبقي (13 حزمة)
```
L0  gtp-types      معرّفات، أخطاء، دلالات، زمن
L1  gtp-wire       التسلسل (إطارات/ترويسات/varint/codec)
L2  gtp-recovery   كشف الفقد، RTT، ACK، السجلات المُرسَلة
    gtp-cc         CUBIC، Pacing، Backpressure
    gtp-scheduler  DRR، مجموعات مرتّبة، جدول حالة
    gtp-path       مقاومة التضخيم، تحقّق المسار، الكوكيز
    gtp-crypto     AEAD، المصافحة، KDF، نافذة الإعادة
L3  gtp-core   ★  محرّك الاتصال — محور التكامل (produce/handle datagram)
L4  gtp-io / gtp-runtime-tokio   التنسيق غير المتزامن
L5  gtp-sim / gtp-cli / gtp      المحاكاة والأدوات والمظلّة
```

### 2.2 مبدأ التصميم الحاكم
**كل انتقال بين الطبقات يمرّ عبر `gtp-core/connection.rs`** — تحديداً دالّتَي `produce_outgoing_datagram` (TX) و`handle_incoming_datagram` (RX). هذا يجعل التكامل مركزياً، لكنه يعني أن **كل درزة تتجسّد كسطر في هاتين الدالّتين**؛ لذا كل متطلبات §6 تُصاغ حول اتساق الحالة عبرهما.

---

## 3. آلية العمل (Mechanism of Operation)

### 3.1 مسار الإرسال (TX) — العقد الوظيفي
`produce_outgoing_datagram(now, out_buf) -> Option<(dest, len)>` ينفّذ بالترتيب:
1. بوابة الحالة (Closed→None، Draining→أكمل الإغلاق).
2. فحص PTO: إن تجاوز الزمن، صرْف السجلات + `cc.on_timeout` + `cc.on_loss` (تسديد دَين inflight — R-1).
3. تقليم العناصر المنتهية من المجدول.
4. تحديث رصيد Pacing.
5. `send_budget = min(cwnd − inflight, رصيد Pacing)`.
6. بناء الترويسة القصيرة + بتة `KEY_PHASE`.
7. استطلاع ACK وإلحاقه **إن** لم يكن الداتاغرام موجَّهاً لعنوان غير المسار النشط (R-7).
8. إطارات التحكّم (توجيه المُتحدّي منفرداً).
9. سحب وترميز عناصر البيانات ضمن الميزانية (Established فقط).
10. `finish` → 11. الختم AEAD → 12. مقاومة التضخيم → 13. تسجيل الإرسال (recovery+cc) → 14. الالتزام بـ ACK بعد الختم (R-3) + تقاعد المفتاح السابق (R-6).
**قاعدة صارمة:** أي خروج فاشل بعد السحب (خطوة 9) يجب أن يمرّ عبر `requeue_popped` (R-4/R-5) — لا فقدان صامت لرسالة موثوقة.

### 3.2 مسار الاستقبال (RX) — العقد الوظيفي
`handle_incoming_datagram(src, datagram, now) -> Vec<ReceivedMessage>`:
عدّ RX → مقاومة التضخيم → فكّ الترويسة → فحص CID → **فحص** نافذة الإعادة (قبل الفكّ) → فكّ التشفير باختيار المفتاح ببتة الطور (R-8) → **الالتزام** بنافذة الإعادة + `mark_validated` + تقاعد المفتاح → تكرار الإطارات → التوزيع حسب الصنف.
**قاعدة صارمة:** الالتزام بنافذة الإعادة **بعد** نجاح المصادقة فقط (SEM-3)؛ حزمة مزوّرة لا تحرق حالة.

### 3.3 المصافحة (Handshake)
X25519 عابر: `ClientHello` (مع إعادة إرسال) → `ServerHello` (كوكي عديم الحالة) → `HandshakeFinish` (إثبات حيازة `client_proof`) → **تأكيد الإنشاء** (PR #2): العميل ينتظر أول ACK قبل اعتبار الاتصال قائماً، والخادم يفهرس حالته على (CID + مفتاح العميل المؤقت).

---

## 4. المكوّنات والخصائص التقنية

| الطبقة | المكوّن | الخصائص التقنية والثوابت |
| :--- | :--- | :--- |
| cc | `CubicCongestionController` | β=0.7، C=0.4، cwnd ابتدائي 10×SMSS، أدنى 2×SMSS، TCP-friendly + Fast Convergence |
| cc | `PacingEngine` | token-bucket، حمل الكسر (CC-9)، burst أقصى 12KB |
| recovery | `LossDetector` | عتبة حزم=3، backoff مسقوف 2^8، سقف ACK 16,384 |
| recovery | `AckTracker` | نافذة احتفاظ 1024، ضغط نطاقات مسقوف 32، فصل peek/commit |
| scheduler | `GameScheduler` | 5 طبقات، DRR، سقف طبقة 512KB |
| scheduler | `StateTable` | FIFO محدود 4096 مفتاحاً (R-2) |
| scheduler | `OrderedGroupReceiver` | سقف 256KB/مجموعة |
| crypto | `GtpAeadProtector` | ChaCha20-Poly1305، nonce من IV⊕(CID‖PN64)، ZeroizeOnDrop (R-6) |
| crypto | `ReplayWindow` | bitmap 128، فصل check/commit |
| path | `AntiAmplificationLimiter` | حدّ 3x (RFC 9000) |

---

## 5. الواجهات والتكاملات

### 5.1 واجهة الإرسال العامة (SDK)
```rust
send_unreliable(payload, priority, deadline, now) -> MessageId
send_sequenced(state_key, sequence, generation, deadline, payload, now) -> MessageId
send_reliable_unordered(payload, priority, deadline, now) -> MessageId
send_reliable_ordered(group_id, payload, priority, deadline, now) -> MessageId
```
**تكامل مطلوب (FR-1):** يجب أن تُرجع هذه الدوال خطأً صريحاً عند تجاوز الحمولة MTU الفعّال (انظر A-1)، لا أن تقبلها.

### 5.2 صيغة السلك (`gtp-wire`)
ترويسة قصيرة (24 بايت) مع بتة `KEY_PHASE`؛ إطارات TLV: Data / ReliableData / Retx / Ack / Ping / Path* / Close / AckFrequency / ClientHello / ServerHello / HandshakeFinish. **قاعدة توافق:** أي تغيير في الصيغة يتطلّب ترقيم إصدار وخطة ترحيل.

### 5.3 زمن التشغيل (`gtp-runtime-tokio`)
`GtpEndpoint::{bind, connect, accept}`؛ حلقتا RX/TX لكل اتصال؛ التوجيه بالـ CID. **تكامل:** التنظيف عند الإغلاق (`is_closed` يُخلي التوجيه).

---

## 6. المتطلبات الوظيفية

> مشتقّة من تدقيق المعمارية (A-1…A-8) وبنود CORE-2 المتبقية. مرتّبة بالأولوية.

### FR-1 — منع تجميد الطبقة بالحمولة الكبيرة (A-1) — P0 — 🟡
- **المشكلة:** حمولة > MTU تُسحَب/تفشل ترميزاً/تُعاد للمقدمة أو لا تُسحَب أبداً، فتُجمّد رأس طابور طبقتها للأبد بلا إشعار.
- **العمل المطلوب (مرحلتان):**
  1. **فوري:** `send_*` ترفض الحمولة التي تفوق (MTU − ترويسة − وسم) بخطأ `PayloadTooLarge` صريح.
  2. **كامل (CORE-2):** تنفيذ التجزئة — `total_fragments`/`fragment_id` فعليان على TX، وإعادة التجميع على RX ضمن مخزن محدود بمهلة.
- **قواعد العمل:** التجزئة تُطبَّق على الأصناف الموثوقة فقط؛ غير الموثوق يُرفض إن فاق MTU.
- **معايير القبول:** (أ) رسالة > MTU على صنف غير موثوق تُرجع خطأً؛ (ب) رسالة موثوقة > MTU تُجزّأ وتصل مُعاد تجميعها؛ (ج) رسالة صغيرة خلف رسالة كبيرة تصل دائماً (لا تجميد) — TEST-1.

### FR-2 — تحديد سعة مجموعات إعادة الترتيب على RX (A-2) — P1 — 🟡
- **المشكلة:** `ordered_groups` (state.rs) بلا حدّ تطبيقي؛ حتى 65,536 مجموعة × 256KB ≈ 16GB يتحكّم بها النظير.
- **العمل:** FIFO محدود لعدد المجموعات النشطة (افتراضي مقترح 256)، إخلاء الأقدم عند التجاوز، تحديث عدّاد البايتات الكلّي.
- **قواعد العمل:** الإخلاء يكلّف طزاجة مجموعة خاملة فقط، لا صحّة.
- **معايير القبول:** استقبال آلاف المعرّفات لا يتجاوز حدّ الذاكرة المُعلَن — TEST-2.

### FR-3 — مصدر حقيقة واحد لـ `inflight` (A-3) — P1 — 🟡
- **المشكلة:** عدّادان مستقلان (`cc.inflight` مقابل `loss_detector.inflight_bytes()`) — جذر انحدار R-1.
- **العمل:** توحيد المصدر: يُشتَقّ `inflight` من `loss_detector` حصراً، ويُلغى عدّاد cc الداخلي (أو العكس بعقد يضمن التماثل عند كل مسار: إرسال/إقرار/فقد/صرف PTO).
- **معايير القبول:** بعد أي تتابع (إرسال/PTO/إقرار)، العدّادان (إن بقيا) متطابقان دائماً — TEST-3.

### FR-4 — توحيد حالة RTT و`active_path` (A-4) — P2 — 🟡
- **المشكلة:** RTT مزدوج (cc مقابل rtt_stats)؛ `active_path` مزدوج (path_validator مقابل hot).
- **العمل:** مصدر RTT واحد يُغذّي pacing وbackpressure وPTO؛ مصدر `active_path` واحد (يُفضّل `path_validator` كمالك، و`hot.active_path` يُشتَقّ منه).
- **معايير القبول:** لا مسار يحدّث أحد النسختين دون الأخرى؛ اختبار هجرة يثبت التطابق — TEST-4.

### FR-5 — مهلة مخزن إعادة الترتيب + بوابة حالة RX (A-6) — P2 — 🟡
- **العمل:** (أ) مهلة لكل مجموعة: حزمة مفقودة تتخطّى أو تُسقِط بعد مهلة مضبوطة (RTT×عامل)؛ (ب) `handle_incoming_datagram` يرفض المعالجة في `Closed`/`Draining` مبكراً.
- **معايير القبول:** مجموعة بحزمة مفقودة دائمة تحرّر مخزنها بعد المهلة — TEST-5.

### FR-6 — تنسيق تدوير المفاتيح داخل النطاق (A-7) — P2 — 🟡
- **المشكلة:** `ratchet_key` يدوي بلا تنسيق؛ `packets_since_ratchet` يُكتب ولا يُقرأ.
- **العمل:** إشارة تدوير داخل النطاق (إطار `KeyUpdate` أو دلالة بتة الطور)، وسياسة تلقائية تقرأ `packets_since_ratchet` (عتبة مقترحة 2^24 حزمة).
- **معايير القبول:** تدوير من طرف واحد يُنسَّق تلقائياً دون فقد اتصال — TEST-6.

### FR-7 — أداء المجدول (A-8) — P2 — 🟡
- **العمل:** (أ) مجموع بايتات لكل طبقة يُحدَّث تفاضلياً (O(1) بدل O(n) في `enqueue`/`requeue`)؛ (ب) DRR حقيقي يمنع تجويع الطبقات الدنيا.
- **معايير القبول:** ملء طابور بآلاف العناصر خطّي لا تربيعي؛ تحت ضغط P1، تحصل P3/P4 على حصّة عادلة — TEST-7.

### FR-8 — عيب تسمية `order_seq` عند التصريف (منخفض) — P3 — 🟡
- **العمل:** العناصر المُصرَّفة من مخزن إعادة الترتيب تُوسَم بتسلسلها الفعلي لا بتسلسل الإطار المُحفِّز.
- **معايير القبول:** تسليم متتابع يحمل كل عنصر `order_seq` الصحيح.

---

## 7. قواعد العمل (Business Rules)

| المعرّف | القاعدة |
| :--- | :--- |
| BR-1 | PTO مسبار لا إعلان فقد — لا يخفّض النافذة مرتين (RFC 9002 §7.5). |
| BR-2 | التفوّق (Supersession) للأصناف المتسلسلة فقط؛ «أحدث بصرامة» يُسقِط الأقدم. |
| BR-3 | لا رسالة موثوقة تُفقَد صامتةً — كل مسار فشل يعيدها للطابور أو يعدّها. |
| BR-4 | ACK لا يُلتزَم به إلا بعد ختم الحزمة ونجاح إرسالها. |
| BR-5 | حزمة موجَّهة لعنوان غير المسار النشط لا تحمل ACK. |
| BR-6 | نافذة الإعادة تُلتزَم بعد المصادقة فقط. |
| BR-7 | المفتاح السابق يُتقاعَد بعد نافذة سماح محدودة (256 داتاغرام) ويُصفَّر. |
| BR-8 | الاتصال لا يُعتبَر قائماً على العميل إلا بعد تأكيد الخادم (PR #2). |

---

## 8. متطلبات الأمان

| المعرّف | المتطلب | الحالة |
| :--- | :--- | :--- |
| SEC-A1 | مفاتيح اتجاهية متمايزة لكل طرف (X25519 + HKDF) | ✅ |
| SEC-A2 | مصادقة AEAD قبل أي أثر جانبي (نافذة الإعادة، الحالة) | ✅ |
| SEC-A3 | كوكي عديم الحالة يثبت ملكية العنوان + إثبات حيازة `client_proof` | ✅ (PR #2) |
| SEC-A4 | حدّ سعة كل بنية RX مفهرسة بمفتاح من السلك | 🔧 جزئي: جدول الحالة ✅ (R-2)، مجموعات الترتيب 🟡 (FR-2) |
| SEC-A5 | مقاومة التضخيم: **مسارية**، بعد المصادقة، تُعاد ضبطها عند الهجرة (A-5) | 🟡 |
| SEC-A6 | تصفير مادة المفاتيح المتقاعدة | ✅ (R-6) |
| SEC-A7 | مصادقة الخادم (MITM) وحماية الترويسة (DEF-1/2) | 🟡 مؤجَّل بقرار معماري |
| SEC-A8 | إهمال السرّ الثابت المنشور مع موعد إزالة | 🟡 |

**متطلب شامل:** كل بنية بيانات على مسار الاستقبال مفهرسة بمفتاح يقرؤه المستقبِل من السلك **يجب** أن تكون محدودة السعة (قاعدة R-2 المعمَّمة).

---

## 9. متطلبات الأداء

| المعرّف | المتطلب | الهدف القابل للقياس |
| :--- | :--- | :--- |
| PERF-1 | `enqueue`/`requeue` بزمن ثابت | O(1) لكل عملية (FR-7) |
| PERF-2 | حساب `inflight` بلا مسح كامل | O(1) (FR-3) |
| PERF-3 | ترميز/فكّ الإطار | بلا نسخ زائد؛ قياس بـ Criterion |
| PERF-4 | ثبات الاتصال الحي عبر الإنترنت | 0% اختناق (تحقّق: 20/20 جولة، تقرير الاختبار الحي) |
| PERF-5 | زمن المصافحة الإضافي (تأكيد الإنشاء) | ≤ RTT واحد على المسار السليم |
| PERF-6 | لا تخصيص ذاكرة غير محدود يتحكّم به النظير | كل بنى RX محدودة (SEC-A4) |

**معيار قياس:** benchmarks بـ Criterion على `seal/open`، `encode/decode`، وتقارب CUBIC، مؤرشفة للكشف الآلي عن الانحدار.

---

## 10. معايير الاختبار والقبول

### 10.1 المبدأ الحاكم
**اختبارات الوحدة خضراء لكنها لا ترى الدرزات.** كل عيب في هذا العمل وقع على درزة. لذا **طبقة اختبار الالتحام إلزامية** وتُقدَّم على غيرها.

### 10.2 سيناريوهات الالتحام (TEST-1…TEST-7)
| المعرّف | السيناريو | الدرزة | يربط |
| :--- | :--- | :--- | :--- |
| TEST-1 | موثوقة > MTU ثم صغيرة خلفها | api↔scheduler↔wire | FR-1 |
| TEST-2 | آلاف `group_id` على RX | wire↔RX | FR-2 |
| TEST-3 | PTO يصرف ثم مقارنة عدّادَي inflight | cc↔recovery | FR-3 |
| TEST-4 | هجرة مسار ومطابقة `active_path`/RTT | path/cc↔core | FR-4 |
| TEST-5 | حزمة مفقودة دائمة وتحرّر المخزن بعد مهلة | scheduler↔core | FR-5 |
| TEST-6 | ratchet من طرف واحد ثم تبادل حركة | crypto↔core | FR-6 |
| TEST-7 | ضغط P1 وقياس إنصاف P3/P4 | scheduler | FR-7 |

### 10.3 قاعدة الإغلاق
**بند بلا اختبار انحدار مُسمّى = غير مُنفَّذ.** كل `FR-x` لا يُغلَق إلا بعد اختبار يفشل عند إزالة إصلاحه (تحقّق سلبي).

### 10.4 بوابات القبول العامة (CI)
- `cargo test --workspace --all-targets` أخضر.
- `cargo clippy --workspace --all-targets -- -D warnings` نظيف.
- `cargo fmt --all -- --check` نظيف.
- `scripts/verify_remediation.sh` → GATE PASSED.
- مهمة fuzz غير حاجبة تبني وتشغّل الأهداف.
- **جديد:** طبقة الالتحام خضراء ومستقرّة (لا flakiness) عبر تشغيلات متكرّرة.

---

## 11. خطة التنفيذ والمتابعة

### 11.1 المراحل (بترتيب الاعتماد)
| المرحلة | المحتوى | مخرَج القبول |
| :--- | :--- | :--- |
| **M0 — دمج الأساس** | دمج PR #1 (R-1…R-8) وPR #2 (المصافحة) بعد المراجعة | main أخضر بكل البوابات |
| **M1 — الاستقرار الحرج** | FR-1 (رفض فوري ثم تجزئة) | TEST-1 أخضر؛ لا تجميد طبقة |
| **M2 — أمن الموارد** | FR-2، إتمام SEC-A4/A5 | TEST-2 أخضر؛ كل بنى RX محدودة |
| **M3 — طبقة الالتحام** | TEST-1…TEST-7 كبنية تحتية دائمة | الطبقة خضراء مستقرّة |
| **M4 — توحيد الحالة** | FR-3، FR-4 | TEST-3/4 أخضر؛ لا مصادر مزدوجة |
| **M5 — المتانة والأداء** | FR-5، FR-6، FR-7، FR-8، PERF-* | الأهداف القابلة للقياس محقّقة |
| **M6 — قرارات معمارية** | SEC-A7 (DEF-1/2)، SEC-A8 (السرّ الثابت) | قرار موثّق + موعد |

**ملاحظة تسلسل:** M3 (الالتحام) تُبنى مبكراً وتسبق M4/M5 — بدونها تُنتج كل مرحلة جيلاً جديداً من عيوب الدرزات.

### 11.2 آلية المتابعة
كل `FR/SEC/PERF/TEST` يُتابَع بحالته (§0) في مصفوفة §12؛ لا يُرفَع بند إلى ✅ إلا باجتياز اختباره المُسمّى وبوابات CI.

---

## 12. مصفوفة التتبّع (Requirement → Test → Priority → Status)

| المتطلب | الوصف | الاختبار | الأولوية | الحالة | الاعتمادية |
| :--- | :--- | :--- | :--- | :--- | :--- |
| FR-1 | منع تجميد الطبقة / تجزئة | TEST-1 | P0 | 🟡 | — |
| FR-2 | سقف مجموعات RX | TEST-2 | P1 | 🟡 | — |
| FR-3 | توحيد inflight | TEST-3 | P1 | 🟡 | — |
| FR-4 | توحيد RTT/active_path | TEST-4 | P2 | 🟡 | — |
| FR-5 | مهلة الترتيب + بوابة RX | TEST-5 | P2 | 🟡 | — |
| FR-6 | تنسيق الـ ratchet | TEST-6 | P2 | 🟡 | — |
| FR-7 | أداء المجدول | TEST-7 | P2 | 🟡 | — |
| FR-8 | تسمية order_seq | ضمن TEST-5 | P3 | 🟡 | — |
| SEC-A4 | حدّ بنى RX | TEST-2 | P1 | 🔧 | FR-2 |
| SEC-A5 | مقاومة تضخيم مسارية | TEST-4 | P2 | 🟡 | FR-4 |
| PERF-1/2 | O(1) للمجدول/inflight | TEST-7/3 | P2 | 🟡 | FR-7/FR-3 |
| M3 | طبقة الالتحام | TEST-1..7 | P1 | 🟡 | M0 |

**بنود مُغلَقة سلفاً (خلفية):** R-1…R-8 ✅ (PR #1)، المصافحة CID+تأكيد الإنشاء ✅ (PR #2)، جدول الحالة R-2 ✅، تصفير المفاتيح R-6 ✅.

---

## 13. المخاطر والافتراضات

| # | المخاطرة | التخفيف |
| :--- | :--- | :--- |
| R-α | التجزئة (FR-1 كامل) تمسّ صيغة السلك | ابدأ بالرفض الصريح (لا كسر توافق)، ثم التجزئة بترقيم إصدار |
| R-β | توحيد inflight (FR-3) يمسّ مساراً حرجاً | تحقّق سلبي إلزامي (TEST-3) قبل الإغلاق |
| R-γ | تنسيق الـ ratchet (FR-6) تغيير في المصافحة/الصيغة | خلف بتة إصدار، توافق للخلف |

**افتراض حاكم:** يُدمَج PR #1 وPR #2 أولاً (M0)؛ كل المراحل التالية تبني عليهما.

---

## 14. الخلاصة

البروتوكول متماسك معمارياً وخوارزمياً وأثبت عمله في بيئة حقيقية. العمل المتبقّي **محدّد ومحصور على الدرزات**، وأخطره FR-1 (تجميد الطبقة). هذه الوثيقة تحوّل كل بند إلى متطلب قابل للتنفيذ والقياس والمتابعة، وتضع **طبقة اختبار الالتحام** في قلب الخطة لأنها وحدها تكسر دورة عيوب الدرزات المتكرّرة. المتابعة آلية عبر مصفوفة §12 وبوابات CI في §10.4.
